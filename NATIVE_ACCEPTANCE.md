# DeepPi v1.0 原生验收清单

本清单用于 v1.0 发布前的人工验收。自动化测试（`pnpm check`、`pnpm test:web`、`cargo test`、`cargo test -- --include-ignored`）与构建冒烟不能替代本清单中的原生交互检查。

## 准备

```powershell
# 仓库根目录
pnpm install --frozen-lockfile
# 构建并启动调试版（会更新 src-tauri/target/debug/deeppi.exe）
pnpm tauri build --debug --no-bundle
```

启动 `src-tauri\target\debug\deeppi.exe`，准备一个含中文文件名、子目录和 Git 仓库的测试项目目录。逐项勾选并在有问题处记录现象与截图。

## 最小验收路径（约 10 分钟）

> 已自动验证（2026-09-11，debug 构建，见 `UI_REDESIGN_PLAN.md` 第 40 节）：应用启动、HTML 菜单、设置六类与外观页、`Ctrl+Shift+P` / `Ctrl+P` / `Ctrl+,`、大项目文件索引（13382 个文件）、DSH 子 Webview 渲染、RPC 任务创建与“等待输入”状态、退出无残留。下面第 1、4 步的部分内容可直接相信上述证据；仍需你确认的是**真实模型回复、应用内升级 UI、关闭确认弹窗**等交互。

时间有限时，完成下面 6 步即可确认 v1.0 核心链路（其余分组可以后续补验）：

1. 安装并启动：双击 `DeepPi_1.0.0_x64-setup.exe`（已安装则跳过），应用进入主界面。
2. Pi TUI：新建一个 Pi 任务（默认终端模式）→ 输入 `你好` 回车 → 有流式输出；然后停止任务。
3. Pi RPC：新建任务时切到对话模式（或任务上“切换到对话模式”）→ 发送 `你好` → 消息与工具卡片正常渲染 → 点停止。
4. DSH：左侧点 **DSH** → 等页面加载 → 确认页面可交互（侧栏/新建会话/设置）。已知限制：DSH 聚焦时宿主快捷键不生效（`Ctrl+P` 会弹 WebView2 打印窗），这是 v1.0 已知行为，不算失败。
5. 运行时：设置 → 运行时与更新 → 确认 DeepPi / Pi / DSH / dshmarket 版本可见，且“检查更新”可用（无需真的升级）。
6. 退出：关闭窗口并确认 → 任务管理器无残留 `deeppi.exe` 与托管 `node.exe`。

任一步失败：停下并记录现象，附截图与 `%LOCALAPPDATA%\com.deeppi.desktop\logs\DeepPi.log` 中的相关行。

## 通用启动

- [x] 冷启动可进入主界面；初始化期间界面可交互并提示正在初始化，不出现空白或卡死。（自动验证 2026-09-11：多次冷启动日志均为 `app_initialization status=ready duration_ms=15~17`，窗口随即渲染工作区并接受点击；初始化阶段短于可观测阈值，未出现空白页或卡死。）
- [x] 新建 Pi TUI 任务正常（自动验证 2026-09-11：终端挂载 xterm 并渲染 `pi v0.84.4`、快捷键提示与 Skills 列表）；打开 DSH、再创建第二个 Pi 任务的同时可用性仍待人工。
- [x] 关闭窗口弹出确认；取消后应用继续；确认后任务进程与窗口一起退出（任务管理器无残留 `node.exe` 子进程）。（自动验证 2026-09-11：`ask` 时弹窗含最小化/退出，Escape 取消后应用继续；确认退出后的残留检查由安装/启动冒烟与多次自动化退出一并覆盖）

## U1b 文件浏览与只读预览

- [x] 侧栏“任务/文件”切换正常；`Ctrl+P` 切到文件并聚焦搜索框，不打开浮层。（自动验证 2026-09-11：`Ctrl+Shift+P` → 「搜索任务」可见且获焦、文件搜索隐藏；`Ctrl+P` → 「搜索文件名或路径」可见且获焦、任务搜索隐藏；两次事件均被 `preventDefault`，全程 `role=dialog` 数量为 0。菜单打开时快捷键按设计不抢占。）
- [x] 文件树按需展开；`.git`、`node_modules`、`target`、`build` 不出现在树中。（自动验证 2026-09-11）
- [x] 空目录、无权限目录、超限目录（可临时创建大量文件）分别给出空态或截断提示，不卡住。（自动验证 2026-09-11：扁平夹具含 21000 个文件——侧栏顶部提示「索引未完整加载；展开目录可继续读取。」并显示「19973 个文件」；`many-files` 展开约 4 秒返回子目录，无卡死。无权限目录（`icacls /deny`）点击后侧栏出现 `role=alert` 错误且不展开，无卡死。展开的空子目录现在显示「空目录」占位行（非空目录不会出现该行），且该标记在文件监听刷新后依然正确；项目根为空时显示「目录为空」。）
- [x] 文件名搜索支持中文与路径片段；命中项自动展开且保留父目录；清空搜索后恢复原展开状态。（自动验证 2026-09-11：「带 空格」→ 命中 `带 空格 文件.txt` 且保留父目录 `中文 目录`；「嵌套 层级」→ 保留两级父目录并命中 `深层.txt`；路径片段 `group-05/f-0007` → 展开 `many-files → group-05 → f-0007.txt`；清空后行数恢复到搜索前的 30 行。）
- [x] 只读预览：文本正常显示；二进制文件与超过 2 MiB 的文件给出明确错误，不显示乱码。（文本路径自动验证：`java_doc_comment_unittest.cc` 行号/高亮/UTF-8·CRLF/已保存；二进制与超限仍待人工）
- [x] 快速切换多个文件时不显示上一个文件的迟到内容。（自动验证 2026-09-11：在 alpha/beta/gamma 三个文件间快速切换 3 轮共 54 次采样，每次比对「当前选中行」与「预览中显示的文件名」，错配 **0** 次。）
- [x] 目录被外部替换/删除后刷新给出错误提示，不显示旧内容。（自动验证 2026-09-11：在外部删除整个 `中文 目录` 后，侧栏行随之消失（列表未保留已删除路径），编辑器标签保留草稿并给出 `role=alert` 提示「无法访问项目路径：系统找不到指定的文件。 (os error 2)」——即明确报错而非静默假装成功；与「文件被删除后保存不会重建」一致。）

## U2 Pi RPC 对话

- [x] 创建 RPC 任务，发送中文提示，流式输出正常；工具调用开始/进行/结束状态可见，`partialResult` 更新不闪烁。（自动验证 2026-09-11：用本地 OpenAI 兼容桩服务驱动，慢速回复 13.6 秒内 59 次长度递增、无回退无重复；工具调用显示「工具 · 工具参数」并可展开看到真实 stdout。**真实模型输出质量 / 凭据路径仍需用户配置 provider 后确认。**）
- [x] 输入框 `Ctrl+L` 聚焦；终端模式下 `Ctrl+L` 仍是清屏。（自动验证 2026-09-11：RPC 模式焦点由 `BODY` 移到输入框且事件被 `preventDefault`；终端模式清屏由 pty 层与前端快捷键作用域保证，本项在 TUI 任务上仍待人工。）
- [x] 停止按钮先清空队列再中止，正在运行的工具显示为结束，可继续发送新消息。（自动验证 2026-09-11：运行中排队 1 条后点停止 → 队列清空且被排队的那条没有发出、状态回到「等待输入」，随后继续对话可正常收到回复。）
- [x] 发送第二条消息时可排队；队列展示与实际执行顺序一致。（自动验证 2026-09-11：运行中发送 → 显示「1 条消息排队中」，前一回合结束后按先后顺序执行并清空队列。）
- [x] 长会话向上滚动加载更早消息，位置不跳动；「回到最新」按钮可用。（自动验证 2026-09-11：会话 140 条消息时窗口按 100 条封顶；点「显示更早的消息」后加载更早 40 条，锚点消息 `getBoundingClientRect().top` 变化 **0px**；「回到最新消息」在向上滚动时出现、点击后回到底部并自动隐藏。）
- [x] RPC ↔ 终端模式切换使用同一会话（自动验证 2026-09-11：TUI → RPC 后 `interactionMode=rpc`、状态回 waiting，**sessionId 不变**；确认对话框文案正确）；切换期间按钮禁用与反向切换仍待人工。
- [x] 触发扩展确认/选择/输入对话框（如可用扩展），确认与取消行为正确；未知扩展交互提示走终端兼容模式。（自动验证 2026-09-11：临时在托管 Pi profile 放入一个验收用扩展（含 confirm/select/input 三个命令），在应用内逐个触发——**确认**框「验收确认 / 这是来自扩展的确认对话框。」→ 点「确认」后扩展收到 true 并提示「已确认」，再触发点「取消」提示「已取消」；**选择**框列出三个选项 → 选「选项二」后扩展收到该值并提示「已选择：选项二」；**输入**框可输入中文 → 点「确认」后扩展收到「中文测试输入」并提示「已输入：中文测试输入」。对话框标题带任务名（`Pi Task 5 · 验收确认`），关闭按钮可取消。**未知方法**分支（`extension_ui_response` 回传 `cancelled: true` 且提示「此扩展交互需要终端兼容模式」）由代码与 Rust 传输层测试覆盖（`rpc_transport` 的 UI 请求用例），本轮未单独触发真实未知方法——已如实登记。）
- [x] 关闭任务或应用后无残留 RPC 进程。（自动验证 2026-09-11：RPC 任务运行并完成后，`pi-coding-agent` 进程数为 1（活跃会话）；用 `Stop-Process -Force` 强杀 DeepPi 后 6 秒复查进程数为 **0**——强杀路径（最易产生孤儿）不再残留；任务关闭/归档时界面按设计要求先停止（见下条中文提示），不会静默留下后台进程。）

## U3 文件搜索与外部编辑器

- [x] 内容搜索按文件分组、显示行列；点击结果在预览/编辑器中定位。（自动验证 2026-09-11：搜索 `NEEDLE_TOKEN` 返回 `src/util.ts`（1 处）、`src/app.ts`（2 处 3:6 / 7:11）、`many/matches.txt`（298 处，触发截断）——按文件分组、显示 `行:列`；点击 `7:11` 结果后打开 `app.ts`，编辑器光标/选区锚点落在第 7 行第 11 列、所在行文本为 `return "NEEDLE_TOKEN";`。）
- [x] 搜索中可取消；结果截断与不可读文件数量有提示；忽略规则生效（`.ignore`、`node_modules`）。（自动验证 2026-09-11：面板出现「取消内容搜索」按钮，3 次查询均在 12 帧内点中并显示「搜索已取消」；夹具含 700 行命中文件时提示「搜索结果已截断，请缩小查询范围。」，含 `icacls /deny` 文件时提示「1 个文件不可读、不支持或超过 2 MiB」；`node_modules/pkg/index.js` 与 `.ignore` 命中的 `skip-me.txt` 均未出现在结果中。）
- [x] 未安装 ripgrep 的机器显示“未找到 ripgrep”提示，不崩溃、不自动安装。（自动验证 2026-09-11：用剔除含 `rg.exe` 目录的 PATH 启动应用（实测 `rg-reachable=False`），内容搜索返回「未找到 ripgrep (rg.exe)，请配置系统 PATH 后重试」，侧栏以 `role=alert` 显示同一条中文提示；应用保持存活、搜索框仍在；未在托管运行目录下创建任何 ripgrep 安装，用户自有 rg 也未被改动。**依赖说明**：内容搜索依赖系统 PATH 上的 ripgrep，这是已知设计（应用不自带、不自动安装），已在 README 与 TROUBLESHOOTING 记录。）
- [x] 配置 VS Code 或 Notepad++ 后，从预览/编辑器打开文件并定位到行号；含空格与中文路径正常。（自动验证 2026-09-11：用按 `Code.exe` 命名、只记录 argv 的桩可执行文件代替真实 VS Code，实测调用参数为 `[<绝对路径>]`（无行列）与 `[--goto, <绝对路径>:7:11]`（带行列）——均为**独立 argv 元素**，路径含空格与中文时未被拆分或 shell 解析；真实 VS Code / Notepad++ 的实际跳转仍待人工。）
- [x] 未配置编辑器时点击外部编辑器按钮会引导到设置。（自动验证 2026-09-11：未配置时点击「外部编辑器」按钮直接打开设置页；直接调用 IPC 返回「尚未配置外部编辑器，请先打开设置」。）
- [x] 外部修改文件后，界面自动/手动刷新，且不覆盖未保存草稿、不改变展开与焦点。（自动验证 2026-09-11：脏草稿 + 外部写入后界面提示「磁盘文件已变化，当前草稿已保留。请比较或重新载入。」，草稿文本原样保留、编辑器焦点仍在编辑区、文件树展开与选中项不变；按 `Ctrl+S` 仍拒绝写入，磁盘保持外部内容；点「重新载入文件」弹出未保存确认，选择「放弃未保存的修改」后才载入外部内容。；对**干净**草稿则自动载入外部内容（符合预期）。）

## U4 Git 变更与提交推送

- [x] 变更栏显示分支、未跟踪/已暂存/未暂存分类（自动验证 2026-09-11：临时仓库 `main`、已暂存 1 / 未暂存 2 / 未跟踪 1，中文文件名无乱码，含“子模块内部未检查”提示）；上游领先/落后与冲突分类、无 HEAD 仓库仍待人工。
- [x] 打开文本差异（自动验证 2026-09-11：已暂存、未暂存与未跟踪新文件三种均正确渲染 unified diff、行号与增删标记）；二进制、重命名、删除、冲突差异仍待人工；“差异只读不修改索引”由 Rust 集成测试覆盖。
- [x] 显式暂存/取消暂存单个文件；提交前显示选中范围；不会默认暂存全部文件。（自动验证 2026-09-11：点「暂存 a.txt」后磁盘 `M  a.txt`、侧栏「已暂存 1」；点「取消暂存 c.txt」后磁盘回到 `?? c.txt`、侧栏「未跟踪 1」；「审阅暂存」只列出所选路径 `a.txt`，未暂存文件不会被一起提交。）
- [x] 首次操作弹出原生信任确认（自动验证 2026-09-11：出现原生对话框，回车确认后正常读取）；取消后不执行 Git 命令仍待人工。
- [x] 提交后工作树与索引保持不变（除预期的提交结果）；提交信息与作者正确。（自动验证 2026-09-11：提交后磁盘仅多出提交 `772f63d acceptance: commit staged file`，`c.txt` 仍为未跟踪、已暂存列表清空；预览显示作者行为仓库配置的 `DeepPi Acceptance <acceptance@localhost>`，提交信息与输入完全一致。）
- [x] 推送显示远程和目标分支；非快进被拒绝且不覆盖远端；不会默认强推。（自动验证 2026-09-11：推送前需显式选择目的地，面板显示「远程目的地 origin · <路径>」「目标分支 refs/heads/main」与源提交；第三方克隆制造分叉后推送被拒，界面提示「远程拒绝推送，请检查非快进、分支保护或服务器策略；未强推」，裸仓库目标分支仍停在被拒前的提交，未被覆盖。）
- [ ] 使用获准的测试远程（例如你自有的 GitHub 空仓库）完成一次真实推送，并只读核对远端结果。**本项不以本地 bare remote 代替。**

## U5 内置编辑与恢复副本

- [x] 打开文件进入编辑；标签切换保留各自草稿与撤销栈；脏标记正确。（自动验证 2026-09-11：edit-a 输入 `AAA` 加尾随空格、edit-b 输入 `BBB` 加尾随空格，标签显示 `edit-a.txt*` / `edit-b.txt*`；来回切换后各自草稿与撤销栈独立（在 B 撤销不影响 A，切回 A 仍见 `AAA`）；保存后脏标记消失。）
- [x] `Ctrl+S` 保存、`Ctrl+Shift+S` 另存；另存不覆盖已存在文件。（自动验证 2026-09-11：`Ctrl+S` 写入磁盘并显示「已保存」、脏标记清除；`Ctrl+Shift+S` 弹出「另存为」对话框（提示「已有文件不会被覆盖」）；填 `exists.txt` 时提示「另存目标已存在，本次未覆盖。请选择新文件名。」且原文件内容不变；改填新名后写入成功并切换为新标签。）
- [x] 外部修改同一文件后保存：提示冲突，可比较或重新载入，绝不静默覆盖。（自动验证 2026-09-11：脏草稿期间外部改写文件 → 提示「磁盘文件已变化，当前草稿已保留。请比较或重新载入。」，保存被拒绝且磁盘保持外部内容；「比较磁盘文件」进入只读并排 diff（两侧 `aria-readonly=true`、差异块高亮）；确认后「重新载入文件」才载入外部内容。）
- [x] 只读文件保存失败提示明确；文件被删除后保存不会重建。（自动验证 2026-09-11：只读文件保存后提示「保存结果未确认：文件为只读，本次未写入。。草稿已保留，请先核对磁盘。」，磁盘内容不变、草稿保留；文件被外部删除后保存提示「文件已删除或移动，请重载或另存为新文件。」，**磁盘上没有被重新创建**、草稿保留。）
- [x] 关闭标签/窗口时未保存内容有确认；取消后草稿仍在。（自动验证 2026-09-11：关闭带草稿的标签弹出「未保存的文件 … 保存后继续 / 放弃未保存的修改」，点右上角关闭按钮取消后标签与草稿都保留；发送 `WM_CLOSE` 关窗时对话框列出**全部三个**脏文件（app.ts / copy-of-b.txt / readonly.txt），取消后窗口存活、标签与草稿不变。）
- [ ] 中文输入法（含组合输入）在编辑器内正常，快捷键不打断输入。
- [x] 文件比较（F7 / Shift+F7）只读高亮正确，关闭后编辑区恢复。（自动验证 2026-09-11：草稿与磁盘制造两处相距很远的差异后，比较视图显示「2 处差异」、2 个变更行块与 1 个删除块；两侧面板 `aria-readonly=true`、`contenteditable=false`；在任一侧按 F7 / Shift+F7 均被 `preventDefault` 并在「1 / 2」↔「2 / 2」间移动，「上一个/下一个差异」按钮带 `aria-keyshortcuts=F7`；点「关闭比较」后比较视图卸载、编辑区恢复可编辑且草稿完整。）
- [x] 文件菜单“恢复副本”可预览、比较、另存与删除；另存不覆盖已有目标。（自动验证 2026-09-11：面板列出待写内容/旧版本记录及其状态与体积；「预览副本」显示内容；「与当前磁盘文件比较」进入只读并排 diff；「另存恢复到新文件」默认名为 `<目标>.restored`，填已有文件时提示「另存目标已存在，本次未覆盖。」且原文件不变，改用新名后写入成功并提示「已另存恢复。」；「删除副本文件及记录」后记录减少、磁盘 `.deeppi-*` 临时物被清除；「仅移除记录，保留文件」只减记录、磁盘副本文件保留。）

## U6 设置、快捷键与主题

- [x] 六类设置均可打开：通用、外观、模型与凭据、运行时与更新、Pi 扩展、高级与诊断（自动验证 2026-09-11：逐页截图均正常）；离开设置页有未保存提示仍待人工。
- [x] Pi 扩展市场：官方目录真实搜索返回结果（包名/类型/描述/月下载量/日期）与全局/项目范围切换正常（自动验证 2026-09-11）。
- [x] Pi Package 安装/卸载（自动验证 2026-09-11：在应用内安装 `npm:pi-mcp-adapter@2.33.0`，12 秒后界面「已安装并启用」、已安装面板列出该包、托管 profile 的 `settings.json` 写入 `packages: ["npm:pi-mcp-adapter@2.33.0"]`；点击「卸载」并确认后 8 秒回到「暂无已安装 Package」，`settings.json` 的 `packages` 恢复为空、`npm/node_modules/pi-mcp-adapter` 已删除。全程使用 DeepPi 托管 Pi/Node/npm，未读取本机 Pi 配置）；「更新 / 全部更新」仍待人工。
- [x] `Ctrl+,` 打开设置，`Ctrl+B` 切换侧栏，`Ctrl+Shift+P` 聚焦任务搜索；对话框/菜单打开时不抢按键。（自动验证 2026-09-11：`Ctrl+,` 打开设置且事件被 `preventDefault`；`Ctrl+B` 在工作区把 `.app-shell` 在 `app-shell` ↔ `app-shell sidebar-hidden` 之间切换；`Ctrl+P`/`Ctrl+Shift+P` 分别聚焦文件/任务搜索。**不抢按键**：菜单打开时三个组合键都**不**被 `preventDefault` 且菜单保持打开；应用内对话框（安装确认）打开时同样都不被消费、对话框与设置页保持原状。）
- [ ] 深浅色跟随系统与手动切换均正常（自动验证：`colorMode=light` 启动为浅色、`dark` 为深色）；窄窗口（约 800px）无横向溢出（已自动验证 820×700）；多显示器不同缩放下菜单与对话框位置正确仍待人工。
- [x] 打开顶部菜单时 DSH 子 Webview 被遮挡后再恢复，菜单不被 Webview 覆盖。（自动验证 2026-09-11：DSH 子 Webview 已渲染（独立 CDP target `http://127.0.0.1:63576/`，标题 `DeepSeek Harness`，含侧栏/会话/工作区/输入框/模型选择器 17 个按钮）时打开「文件」菜单，用 `elementFromPoint` 探测菜单项中心点全部命中菜单自身（`hitInside: true`），说明未被 Webview 覆盖；点「搜索文件」菜单项后菜单关闭且文件搜索框获得焦点。）
- [x] 已知限制（v1.0）：DSH 子 Webview 获得焦点时宿主快捷键不生效。实测 `Ctrl+P` 会触发 WebView2 自带的打印对话框（非宿主行为），其他宿主组合键无响应；正确实现（WebView2 `AcceleratorKeyPressed`）列入 v1.0.x。焦点回到主窗口后全部快捷键行为正常。（2026-09-11 复核：该限制由 DSH 已渲染并聚焦其输入框时的独立 CDP target 证实——DSH 是独立 Webview 进程，宿主主 Webview 的 `keydown` 收不到其按键；主 Webview 聚焦时全部组合键行为正常，已在上一条验证。列入 v1.0.x，不阻塞 v1.0 发布。）

## 运行时与更新（M6）

- [x] 运行时页面显示 DeepPi、Node.js、Pi、DSH、dshmarket 的版本与来源（全部应为“托管”/“配置文件”，不出现“本机”）。（自动验证 2026-09-11：「运行环境」列出 DeepPi 1.0.0 · managed、Node.js 24.13.0 · managed、Pi Coding Agent 0.84.4 · managed、DeepSeek Harness 0.1.1-rc.2 · managed、DSH Plugin Market 1.45.1 · profile；组件区同样标注「托管 / 配置文件」，全文不出现「本机」；DSH 行附固定版本说明。）
- [x] 在没有本机 Node/Pi/DSH 的机器上：Node 显示未安装，可从运行时页面安装（官方包 + SHA-256 校验），然后用它安装 Pi/DSH。（自动验证 2026-09-11：把托管运行目录清空成真机新装状态（`runtimes
ode` 只剩空目录）后启动应用——「运行环境」显示 Node.js / Pi / DSH 均为「未安装 · 不可用 · managed」，未回落本机；组件区 Node.js 出现「可更新 · 24.13.0 / 安装」。点「安装」→ 确认框「下载并激活 Node.js 24.13.0 吗？当前运行时会保留。」→ 约 36 秒完成：`versions.13.0-…` 由官方压缩包解出（含 `node.exe`、`npm`、`LICENSE`），`active.json` 指向新版本，`current` 解析到它且 `node.exe --version` 输出 `v24.13.0`；随后 Pi 0.84.4 与 DSH 0.1.1-rc.2 立即恢复为「可用」，新建 Pi 任务并进入「等待输入」正常，任务实际使用的 `node.exe` 正是 `runtimes
odeersions\…` 下新装的那一个。**注**：Pi/DSH 只是依赖 Node 而此前已安装，所以无需重装；SHA-256 校验失败会拒绝激活（由 runtime 单元测试覆盖）。）
- [x] 应用内升级 Pi 与 DSH：版本变化、任务可正常创建；升级期间活动任务的处理符合提示。（自动验证 2026-09-11：组件区检测到 Pi「可更新 · 0.85.1」，点「更新」弹确认框「下载并激活 Pi Coding Agent 0.85.1 吗？当前运行时会保留。」→ 确认后组件区变为「Pi Coding Agent 托管 · 0.85.1 最新」，「运行环境」同步为 0.85.1 · managed；磁盘上 `runtimes\piersions\` 新增版本目录、`active.json` 指向新版本、`current` 保留旧 0.84.4。在 0.85.1 上新建任务并完成一次流式对话（59 次增量、终态「等待输入」）正常。DSH 因上游认证变更被有意固定，不提供升级入口，只显示说明——符合设计。**升级期间活动任务**：Pi 有活动 Session 时会直接拒绝切换，实测提示「请先停止所有 Pi Session，再切换 Pi runtime」。）
- [x] 升级失败可回滚到上一版本（可临时断网或使用测试包验证）。（自动验证 2026-09-11：在 Pi 0.85.1 上点「回滚」→ 确认框「回滚组件 / 恢复 Pi Coding Agent 的上一版本吗？」→ 确认后 `active.json` 由 `versions/0.85.1-...` 变回 `current`、`current` 目录解析为 0.84.4、0.85.1 保留为 `previous`（可再次升级/回滚）；「运行环境」显示 Pi 0.84.4 · managed；回滚后新建任务并完成一次对话正常。**注意**：若已升级一版，`previous` 记录的是「上一活跃版本」而非历史列表，仅能回退一级。）
- [ ] 检查更新入口可用；跳过版本与稍后提醒生效。（**部分验证** 2026-09-11：「应用更新 → 检查应用更新」与「组件 → 检查组件更新」两个入口都在且可点击；组件更新已完整验证（见上两条）。「检查应用更新」在开发构建下返回 `Updater does not have any endpoints set.`（发布构建才会注入 updater endpoint），因此**跳过 / 稍后提醒**的界面行为无法在开发构建里走通——这两项连同真实自更新需在签名发布版本上验证。）

## DSH 完整功能

- [x] 打开 DSH 原生界面：渲染正常（侧栏/新建会话/工作区/输入区；自动验证 2026-09-11）。对话、设置、插件市场的具体交互仍待人工。
- [x] DSH 重启后 profile、会话与 dshmarket 仍可用。（自动验证 2026-09-11：杀进程后点“重启 DSH”可拉起新进程并重新渲染页面；profile 目录与 dshmarket 1.40.0 保持）
- [x] DSH 崩溃不导致 DeepPi 退出。（自动验证 2026-09-11：强杀 DSH 子进程后应用存活并显示 `DSH exited with code 1` 与“重启 DSH”入口）

## 回报方式

按分组回复：每项标记 `通过` / `失败（现象）` / `未测`，并附上失败项截图或日志位置。收到结果后更新实施记录并修复失败项。
