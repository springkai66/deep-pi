# DeepPi 桌面工作台重设计开发计划

确认日期：2026-09-08。状态：开始实施，尚未完成整体迁移。

## 1. 决策与范围

本计划覆盖 DESIGN.md 中旧 MVP 的 UI 非目标：允许增加 Pi RPC 对话、文件浏览与编辑、Git 审阅能力。旧版终端宿主作为兼容路径保留，不删除原生会话、不重写运行时与凭据安全机制。旧 M0-M7 的完成记录不代表本计划完成。

- 顶部：文件、编辑、视图、设置、帮助菜单。左侧窄导航：Pi、DSH、底部设置。
- Pi：项目侧栏（任务/文件）、中央任务或文件、可收起的右侧变更栏、按需展开的终端/日志。
- DSH：继续完整原生 Web UI；不叠加 Pi 侧栏，不注入重设计样式，不开放宿主 IPC。
- 搜索始终位于侧栏顶部，不增加 Ctrl+P 浮层。文件名过滤与全文搜索为不同能力。
- 设置独立页面：通用、外观、模型与凭据、运行时与更新、Pi 扩展、高级与诊断。
- 所有按钮接真实能力；尚未实现的 Git、文件保存和 RPC 不以演示数据冒充。

## 2. 分阶段实施

### U1a：应用壳与任务导航（代码已实现，原生交互待验收）

- [x] 顶层五类菜单，复用现有项目、任务、设置、模型、扩展和更新操作。
- [x] 全局 Pi/DSH/设置导航；设置隐藏项目侧栏；侧栏可收起。
- [x] 任务常驻搜索，大小写不敏感的子序列匹配，支持中文，包含归档任务。
- [x] Ctrl+Shift+P 聚焦并选中任务搜索；Ctrl+B 切换侧栏；Ctrl+, 打开设置。
- [x] 菜单关闭、键盘访问、输入法组合输入保护；模态对话框优先处理键盘。
- [x] DSH 菜单展开时隐藏原生子 Webview，关闭后恢复，切换时校准边界（实现完成，原生覆盖关系仍待验收）。
- [x] 保留真实 TUI 主区，明确不等于 RPC 已完成。

验收：现有任务创建/打开/停止/归档等调用不变；搜索无匹配有空状态；搜索 Escape 清空，上下键定位结果，Enter 打开；侧栏收起不停止任务；类型检查、单元测试、生产构建通过。原生子 Webview 焦点单独验收。

### U1b：项目文件浏览与只读预览

- [ ] 已选项目下增加任务/文件切换、常驻文件搜索；Ctrl+P 切到文件并聚焦搜索，不打开浮层。
- [ ] Rust 按项目 ID 从 TaskStore 解析根目录，不接受任意绝对路径授权。
- [ ] 有界目录遍历：排除 .git、node_modules、构建目录；按需展开；支持刷新、空目录、权限错误和截断提示。
- [ ] 文件名/相对路径模糊过滤，保留父目录，搜索展开命中路径，清空恢复原展开状态。
- [ ] 只读预览：限制文件大小、识别二进制/编码错误；切换项目或快速切换文件时忽略过期结果。
- [ ] 拒绝目录穿越、符号链接和 Windows reparse point 越界；读取前后检查身份，明确竞态防护边界。

拟定命令：list_project_files(projectId, relativePath)、read_project_file(projectId, relativePath)。返回相对路径、类型、大小、截断标志；错误可定位，不泄露凭据内容。阻塞 I/O 进入 spawn_blocking。

验收：临时项目测试正常读取、../、绝对路径、链接、二进制、超限、目录消失；搜索覆盖路径、中文、无结果、清空恢复；不得在每次输入时全盘扫描。

### U2：Pi RPC 纵向切片

- [ ] 按当前已安装 Pi 的官方 RPC 文档确认命令和事件，锁定兼容基线。
- [ ] Rust 独立会话传输：stdin/stdout JSONL，仅 LF 分帧，requestId 对应响应；stderr 独立有界日志。
- [ ] taskId + runId 过滤过期事件，区分请求响应和事件；断开/退出释放订阅。
- [ ] 中央结构化消息、工具执行、累计 partialResult、发送/排队/停止和失败恢复。
- [ ] 停止考虑 clear_queue + abort；Ctrl+L 聚焦真正对话输入，不抢 TUI 清屏键。
- [ ] 支持 extension_ui_request 的确认/选择/输入；不支持的自定义 TUI 明确走兼容模式。
- [ ] 读取原生会话，保留 TUI 入口；迁移不重复存储原生消息。

拟定边界：rpc_session 管进程/请求映射，前端 reducer 管消息状态；事件携带 taskId/runId。与 PTY 分离但共享任务索引与停止生命周期。

验收：分帧/中文/大消息/部分输出/乱序/退出/取消测试；真实 Pi 完成发送、工具调用、停止、恢复会话；凭据不入前端日志。

### U3：文件搜索与外部编辑器

- [ ] 全文搜索独立入口；使用成熟搜索工具，先探测可用性，不自动安装。
- [ ] 搜索结果按文件分组，定位行列；有界结果、取消、忽略规则和大仓库性能。
- [ ] 配置外部编辑器，参数数组启动，不经 shell 拼接路径；打开文件及行号。
- [ ] 文件变更刷新，合并重复通知，不覆盖用户焦点和展开状态。

验收：含空格/中文/特殊符号路径、安全参数、编辑器缺失、取消及大仓库测试。

### U4：Git 变更与提交推送

- [ ] 右侧真实工作区状态、分支、远程、未跟踪/已暂存/未暂存分类。
- [ ] diff 覆盖文本、二进制、重命名、删除、冲突及无 HEAD 仓库。
- [ ] 显式暂存/取消暂存，提交与推送分开，不默认暂存所有文件。
- [ ] 提交前显示选中范围；推送显示远程和目标分支；不默认强推。
- [ ] Git 参数数组调用，有界输出/超时/取消；复用系统凭据，不读取展示令牌。
- [ ] 不把已有用户改动标记成 Agent 修改；处理并发索引变更和锁冲突。

验收：临时本地仓库及 bare remote 完成提交/推送测试；另做获准远程测试，不以本地测试冒充 GitHub 成功。

### U5：轻量内置编辑

- [ ] 选择成熟编辑器组件，验证包体积、IME、主题和可访问性后接入。
- [ ] 标签页、脏标记、保存、另存及关闭确认；外部 IDE 入口继续保留。
- [ ] 读取时返回内容版本，保存使用 expectedVersion；冲突提示比较/重载，禁止静默覆盖。
- [ ] 原子写入、编码与换行保留；与 Agent/Git 刷新协调。

验收：外部并发修改、删除、权限失败、取消关闭、多文件脏状态、中文输入、超限文件测试。

### U6：设置整理与整体验收

- [ ] 六类设置迁移，保留现有持久化字段、运行时取消/回滚及 Credential Manager。
- [ ] DSH 原生 Webview 内焦点下的宿主快捷键采用受限原生路由，不授予 IPC 权限。
- [ ] 快捷键展示与实现共享定义，检查终端/编辑器/IME/模态冲突。
- [ ] 深浅色、窄窗口、缩放、多显示器 DPI；菜单不被子 Webview 遮挡。
- [ ] 单元/集成/原生端到端、性能与 8 小时长稳；发布签名和真实 tagged release 更新仍单独验收。

## 3. 顺序与交付约束

U1a -> U1b；随后 U2 和 U3 可独立推进；U4 依赖文件与进程安全边界；U5 依赖只读文件契约；U6 每阶段持续补验，最终收口。

每个阶段先写行为测试再实现。验证使用 pnpm test:web、pnpm check、pnpm build；修改 Rust 时增加针对性 cargo test 和 clippy。涉及进程/权限/Webview 时必须做原生验证，普通浏览器验证不替代 Tauri IPC 验收。

按阶段持续实施，不将后续勾选为完成，不自动提交或推送。2026-09-08 用户已进一步要求完整实施本计划；阶段交付不代表整体目标完成。

## 4. U1a 验证记录

2026-09-08：

- 新增 navigation 测试先因实现缺失失败，随后前端全部 16 项测试通过。
- pnpm check：0 错误、0 警告。
- pnpm build 与 pnpm tauri build --debug --no-bundle 通过，生成 src-tauri/target/debug/deeppi.exe。原生链接器有创建导入库的提示，不影响构建。
- 浏览器实际界面：搜索无结果、Escape 清空、Ctrl+Shift+P 选中搜索词、菜单隐藏侧栏、快捷键展开侧栏、Ctrl+, 打开独立设置页通过。
- 1280px 桌面与 390px 窄窗口截图检查；390px 下页面 scrollWidth 等于 clientWidth，帮助菜单不越界。
- 普通浏览器不启动 Tauri IPC 初始化，不提供演示项目/任务。该验证不覆盖真实任务结果列表、原生中文 IME、TUI 焦点或 DSH 子 Webview。
- 待原生验收：DSH 展开菜单/模态时遮挡与焦点、DSH 启动中切回 Pi、活动 TUI 切设置后恢复、真实任务搜索 Enter 打开与上下键导航。
- Rust 后端没有改动，本切片未重跑真实运行时下载安装测试，不将其计作本轮通过项。

## 5. U1b 实施记录

已接入 list_project_files(projectId, relativePath, recursive) 与 read_project_file(projectId, relativePath)，仅主 Webview 可调用，项目根目录从未移除的项目记录解析。

- 文件侧栏、项目选择、Ctrl+P 聚焦、常驻路径模糊搜索、父目录保留与搜索展开已实现；清空恢复原展开集合。
- 目录索引最多访问 20,000 个条目、最多递归 32 层；3 秒协作式扫描预算，不把预算误称为单次阻塞系统调用的硬超时。
- 首次索引后按需补读目录，过滤仅操作内存；列表按固定行高虚拟化，避免生成数万个 DOM 节点。
- 预览最多 2 MiB UTF-8；二进制/不支持编码明确报错；保留 BOM 与 CRLF；过期的项目/文件请求不覆盖新选择。
- Windows 为路径各级祖先与最终对象持有不共享写入/删除的句柄，使用 OPEN_REPARSE_POINT 拒绝重解析点，不依赖一次 canonicalize 检查。当前其他平台失败关闭，不静默使用弱化实现。
- 实际测试：6 项文件安全测试通过，包含目录联接、项目根被联接替换、读期间目录重命名与文件写入拒绝、无关文件仍可写。符号链接创建因 Windows 特权 1314 失败，改用无需该特权的真实目录联接测试相同的重解析点拒绝路径，没有将失败用例标成跳过或成功。
- 前端全部 19 项测试通过；类型检查零错误零警告；Clippy -D warnings 通过；原生程序构建并启动成功。
- 顺带修复：终端组件独立于设置/DSH/文件预览的展示分支持久挂载，切换视图不再卸载终端组件。
- 未完成验收：真实文件结果的原生键盘、IME 和大仓库体验。桌面工具连续检测到用户输入，已停止自动接管窗口，不能据启动截图声称整个闭环通过。

## 6. U2 实施与验证记录

2026-09-08，已实现主链路，阶段尚未验收完成：

- 独立 RPC 进程传输，LF JSONL 分帧、跨块 UTF-8、请求 UUID、超时释放、有界队列和事件回放游标；taskId/runId 过滤过期运行。
- 新任务默认 RPC 对话，保留终端任务和 RPC 切到同一原生会话的 TUI 入口；共享任务并发限制、运行时互斥和停止生命周期。
- 消息、累计工具输出、模型选择、排队/引导、clear_queue 与 abort、扩展确认/选择/输入/编辑对话框已接入。
- stderr 与 JSON 协议分开，内存保留最多 256 KiB 诊断内容；尚未提供脱敏诊断展示/导出，不能视为完整诊断功能。
- 正式工具结果到达后释放重复工具输出；退出后清除排队状态并结束工具执行标记。
- 没有通过静默裁剪旧消息实现所谓的长会话优化；完整历史分页、Markdown/代码显示及长稳测试仍未完成。
- 真实 Pi 集成测试通过本地 SSE 模型夹具完成 read 工具调用、结果回传和相同 session ID 重启恢复，不使用云模型凭据或产生云调用费用。
- 上一完整验证：Rust 91 项通过、0 失败、0 跳过，包含联网模型目录、运行时下载安装/验证/回滚；前端 30 项通过，类型检查、Clippy 和桌面构建通过。
- 原生启动验收被物理 Escape 中止，已停止自动操作；不将浏览器或构建结果代替原生发送/取消/扩展交互验收。
- 双向模式切换已实现：视图菜单和两种任务标题栏均提供入口，确认后停止当前 run，再使用同一任务/session ID 启动目标模式。重复点击、停止失败、重启失败、确认期间 run 变化均有回归用例。
- 新增 stop_pi_run(taskId, runId)，在后台等待 PTY 进程、输出和控制台清理后再返回；5 秒等待超时保留运行占位并报错，不允许切换启动第二个进程。RPC/TUI 两侧启动都检查另一管理器的当前任务。
- 已结束 RPC 的重复停止返回成功；旧 run 的停止仍拒绝。终端退出回调向页面传递原始 runId，防止组件卸载前的旧退出事件污染新运行。
- 切换期间暂时禁止该任务再次重启/停止/归档/删除和输入，窗口关闭要求先完成切换。原生双向往返、首次终端按需加载焦点仍待验收。
- 双向切换阶段验证：Rust 97 项通过、0 失败、0 跳过，包含真实 PTY 停止/超时/清理及 RPC 代际测试、联网运行时测试；前端 38 项通过，原有终端测试保留；类型检查、Clippy 和桌面构建通过。

## 7. U3 实施与验证记录

2026-09-08，功能已开始接入，阶段尚未完成：

- 文件树顶部保留文件名过滤，增加独立内容模式；Ctrl+P 回到文件名模式，无快速打开浮层。
- 内容搜索使用本机 PATH 中已安装的 ripgrep，不自动安装；默认字面匹配，支持中文路径、大小写不敏感及行列位置。
- 先按 ripgrep 忽略规则枚举候选，再使用安全句柄固定每批文件和祖先目录；排除生成目录，拒绝 reparse point、二进制、不支持编码及超过 2 MiB 的文件。
- 可执行文件来自项目外，持有句柄并拒绝重解析路径和项目内工具；不通过 shell 拼接命令。
- 最多 500 个命中、每次进程输出捕获最多 64 KiB，超出明确提示截断。搜索预算为 10 秒；文件系统调用、进程启动和管道清理可能额外耗时，不声称硬实时截止。
- 独立搜索取消管理器与前端最新请求控制器：串行运行、清空/切项目/切模式后旧结果不回写；手动取消可用。
- 搜索结果按文件分组，可从搜索框用方向键进入结果，再用上下/Home/End 定位、Enter 打开、Escape 清空；打开预览时转换 Unicode 列号为 UTF-16 文本位置，保留 CRLF 和 BOM。
- 外部编辑器配置支持 VS Code/Insiders 与 Notepad++；显式选择对应绝对 .exe 路径，独立保存，不被旧外观设置快照覆盖。预览页提供外部打开或配置入口。
- 编辑器启动使用参数数组、空标准流和编辑器安装目录作为工作目录，不加入 Pi 任务进程树；启动成功仅表示已发送打开请求，不等于编辑器已经完成加载。
- 返回应用、当前项目任务结束时刷新文件树与只读预览；文件树刷新合并重复通知且保留展开状态，预览保留滚动位置。尚无持续目录监听，其他时刻的外部变更可手动刷新。
- 已验证内容搜索的字面匹配/中文列号、目录联接拒绝、忽略规则/大小限制、截断/取消，以及前端旧请求覆盖和行列转换；编辑器参数和配置迁移/防覆盖测试已通过。
- 浏览器检查编辑器表单、按钮启用、390px 窄布局通过，无横向溢出或控制台错误；此项不覆盖 Tauri 文件对话框、外部编辑器启动和原生定位。
- 本阶段完整验证：Rust 94 项通过、0 失败、0 跳过（包含真实联网安装/回滚及 Pi 工具会话测试）；前端 31 项通过；Svelte 检查零错误零警告、Clippy -D warnings 和最新桌面构建通过。
- 文件索引刷新也重新执行当前内容查询；同一查询的旧结果在新结果到达前保留，以避免自动刷新销毁用户焦点。
- 待完成：搜索结果键盘的原生验收、持续文件变化通知评估、大仓库性能及真实外部编辑器往返验收。

## 8. 剩余交付与限制

- U2 尚有显示、诊断、长会话收口及双向切换原生验收；U3 尚有完整刷新评估与原生往返验收。
- U4 Git 状态、差异、暂存、提交和推送；U5 内置编辑与冲突安全保存；U6 六类设置、原生快捷键、缩放/DPI/长稳验收均不能标为完成。
- 已将终端组件改为首次存在 TUI 任务时动态加载，保留已挂载终端不随视图切换卸载。构建清单确认主页面业务 JS 约 172.70 kB（gzip 55.54 kB），终端动态块约 338.45 kB（gzip 86.56 kB），不包含其他共享依赖的体积；500 kB 分包警告消失，未修改警告阈值。
- 按需加载后的原生首次 TUI 订阅与焦点时序仍需验收。构建仍有 SSR Channel 未使用和 Windows 链接器生成导入库提示，未将其误报为零构建警告。
- 当前没有提交或推送本轮 UI 修改。

## 9. U4 状态读取切片

2026-09-08，已接入真实状态读取，U4 整体仍未完成：

- 顶部工具栏与“视图”菜单提供变更栏入口；宽屏为可收起的右侧栏，1100px 以下为右侧抽屉，不挤压中央任务列。
- project_git_status 根据项目 ID 读取授权根，只允许 main Webview；当前明确拒绝项目子目录、根为 .git 文件的 linked worktree 和重解析目录，不向上搜索其他仓库。
- 首次执行由 Rust 发起原生信任确认，并在当前应用进程内按项目路径保留授权。前端不能用“已信任”布尔值替代确认；后台刷新不发起新的授权弹窗。
- Git 仍使用用户和仓库配置，信任提示明确说明配置的文件过滤器可能执行；禁用 fsmonitor、optional locks 和交互凭据提示，清除继承的 GIT_* 仓库重定向环境，使用项目外固定句柄的 git.exe 与参数数组。
- porcelain v2 NUL 分隔解析，保留空格、中文和重命名原路径；分开显示冲突、已暂存、未暂存、未跟踪，同一文件可以同时属于两个修改组。
- 显示分支、无 HEAD、分离 HEAD、上游及领先/落后数量。上游数量依据本地引用，没有 fetch，也不代表远程实时状态；远程列表和推送目标尚未接入。
- 当前忽略子模块内部检查，界面明确提示，不把该局部结果称为整个仓库干净。非 UTF-8 路径明确报错，不通过有损转换制造可操作的错误路径。
- 输出捕获上限 64 KiB；截断时拒绝展示不完整状态。进程等待预算 10 秒，使用现有 Job 子进程树收尾；本切片尚无用户取消按钮和大仓库增量状态。
- 前端请求串行合并刷新；切项目、隐藏面板、销毁组件后旧请求不回写。失败可手动重试，未给差异、暂存、提交、推送放置假按钮。
- 验证：Rust 103 项通过、0 失败、0 跳过，包含联网运行时测试；前端 43 项通过；Svelte 检查零错误零警告、Clippy -D warnings 与桌面构建通过。
- 新增真实临时仓库测试证明中文文件同时已暂存/未暂存状态、无 HEAD、读取不改写索引和拒绝发现父仓库；未提交或推送当前开发仓库。
- 浏览器 1440px / 390px 布局、变更栏开关及无横向溢出检查通过，控制台无错误；浏览器无生产项目数据，此项不覆盖真实变更列表和 Tauri 原生授权弹窗。
- 后续仍需差异审阅、显式暂存/取消暂存、提交前范围确认、推送目标确认、本地 bare remote 闭环，以及获准的真实远程和原生交互验收。

## 10. U4 差异审阅切片

2026-09-08，已接入只读差异链路，替代第 9 节中“尚无差异入口”和“拒绝所有子目录/linked worktree”的阶段限制：

- 右侧变更项可打开中央统一差异；区分已暂存、未暂存、新文件与冲突。打开差异隐藏任务内容但不卸载或停止 RPC/PTY；打开任务、文件或聚焦输入时关闭差异。
- GitRepository 定位项目所属工作树、独立 Git 元数据与公共元数据目录，固定目录、.git 标识和 commondir 文件句柄并二次核验。授权按项目及解析后的三类路径绑定，不再仅按前端布尔值授权。
- 子目录项目的 status 使用明确路径范围，返回项目相对路径；差异请求重新校验路径和分组。linked worktree 使用独立索引、显式工作树及公共元数据路径。
- 仓库定位使用只读 rev-parse；状态和差异执行前要求原生信任确认。Git 仍使用受信任的用户/仓库配置，不能据禁用外部 diff/textconv 声称禁止了所有配置过滤器执行。
- Git 负责差异计算；禁用外部 diff、textconv、颜色、相对路径配置和子模块内部比较。新文件通过 no-index 与空输入比较；真实 Windows 测试证实此 Git 的 /dev/null 与退出码 1 处理正常。
- 覆盖无 HEAD 的暂存内容、空新文件元数据、删除、重命名、二进制、非 UTF-8 和三种冲突基准。冲突比较不修改索引、不自动标记已解决。
- 前端统一差异保留双侧行号、增删标记和元数据；文本插值不执行 HTML。22px 固定行高虚拟化，长差异实际仅挂载可视区及缓冲行；支持方向键、Home/End、PageUp/PageDown。
- 同项目刷新合并请求并保留旧内容，切文件后旧响应不回写；后端串行化状态与差异，避免两个面板同时刷新造成忙错误。队列的主动取消与总等待预算仍待完善。
- 状态输出仍限 64 KiB；差异截断时明确标记部分显示。工作区现有文件超过 8 MiB 拒绝，非 UTF-8 不进行有损转换。未声称大文件或大仓库完整验收。
- 验证：Rust 112 项通过、0 失败、0 跳过，含联网模型目录、运行时隔离安装/验证/回滚及真实 Pi 测试；前端 47 项通过；Svelte 检查零错误零警告、Clippy -D warnings 和桌面构建通过。原有 SSR Channel 与 Windows 链接器提示仍存在。
- tests/ui 提供与生产路由隔离的真实组件测试页，使用本机 1421 端口；夹具不进入正式应用。浏览器验证文本转义、二进制、错误恢复、冲突基准、慢/快请求切换及长差异末行可达；390px 无横向溢出，关闭按钮可见。该组件测试不冒充原生 IPC 或真实 Git 列表端到端验收。
- 待续：子模块内部范围、跨边界重命名暂存策略、文件在缺失/重建期间的差异快照一致性、授权目录被原地替换的身份失效策略、主动取消与大仓库预算；随后显式暂存/取消暂存、提交与推送闭环。整个 U4、U5、U6 仍未完成。

## 11. Git 授权身份修复

2026-09-08：

- 修复已授权仓库在同一路径被替换后仍复用原授权的问题。先以真实临时仓库替换 .git 目录复现失败，再实现句柄身份绑定。
- GuardedPath 通过 Windows GetFileInformationByHandleEx(FileIdInfo) 从已持有句柄获取卷序列号与 128 位文件 ID；无法获取时失败关闭，不降级为路径字符串授权。
- RepositoryIdentity 同时包含项目、工作树、Git 元数据及公共元数据路径，以及这些目录和已有 .git/commondir 标识的文件身份。路径不变但目录或标识被替换后，需要重新原生确认。
- 测试覆盖 .git 目录同路径替换、子目录项目同路径替换、普通文件编辑后重新打开继续复用有效授权。此修复不声称检测所有配置内容变化，也不替代受信任项目的配置执行提示。
- 验证：Rust 115 项通过、0 失败、0 跳过（包含联网用例）；前端 47 项通过；Clippy -D warnings 与桌面构建通过。本轮没有 UI 行为修改，未重新操作原生窗口。
- Git 写入、取消、差异快照一致性、子模块及其余 U2/U5/U6 工作仍未完成，未提交或推送当前开发仓库。

## 12. 窗口关闭修复与 Git 读取取消

2026-09-08，优先处理用户报告的确认退出后无法关闭：

- 根因：原关闭流程调用窗口 destroy，但 core:default 并未授权 destroy/minimize。新增 main-window-lifecycle capability，只匹配 main Webview，不向 DSH 或远程页面开放权限。
- 关闭逻辑提取为可测试处理器，每次关闭事件先 preventDefault；确认开始即防重入，避免重复点击叠加确认或绕过停止流程。
- 取消、最小化失败、任务停止失败或 destroy 失败后释放处理中标记，允许重试；任务模式切换期间继续阻止退出。
- 新版桌面构建成功，生成 src-tauri/target/debug/deeppi.exe。生成能力清单确认包含上述两项 main-only 权限。运行中的旧程序需要重新启动新版才能加载新权限。
- 未自动接管原生窗口，实际点击关闭与最小化仍待原生验收；不以单元测试或构建替代该验收。

同时继续 U4：

- 状态与差异读取采用独立操作 UUID；支持主动取消，切换项目、文件、隐藏或销毁视图时取消旧请求，旧结果不覆盖当前选择。
- 后端注册表限制并发请求，保留有界、带过期时间的取消记录，处理取消 IPC 早于读取注册的竞态并拒绝近期 ID 重用。
- 等待仓库锁可取消；总协作式预算为 30 秒，包含排队、仓库定位及 Git 调用，单次进程最长 10 秒且不超过剩余预算。复用 Job 子进程树停止机制。
- 原生信任弹窗不会被自动关闭，返回后再次检查预算；阻塞文件系统调用也不保证硬实时。因此不将 30 秒描述为所有场景的墙钟上限。
- 浏览器真实差异组件夹具验证：慢请求显示取消按钮，取消后旧响应不回写，刷新可以重试。夹具取消不终止真实 Git 进程；后端进程取消另由 Rust 测试覆盖。
- 完整验证：前端 56 项通过；Rust 121 项通过、0 失败、0 跳过，包含联网模型目录、运行时隔离安装/验证/回滚及真实 Pi 工具会话恢复。类型检查零错误零警告，Clippy -D warnings 和桌面构建通过。
- 构建仍有 SSR Channel 未使用与 Windows 链接器生成导入库提示；浏览器工具自身的遥测请求超时不计作应用功能测试。
- 下一步继续显式暂存/取消暂存及写入范围保护，再接提交范围确认和推送目标确认。U4 写入、U5 编辑、U6 设置与其余验收仍未完成，未提交或推送本轮修改。

## 13. U4 显式索引操作

2026-09-08，暂存、取消暂存和冲突标记已接入，提交/推送仍待实现：

- 每个变更项旁增加独立图标操作：暂存、取消暂存、标记已解决；按钮与差异入口分离，有标签、悬停提示、固定尺寸与禁用状态。没有默认暂存所有文件。
- 新增 project_git_change_index，仅 main Webview 可调用，项目路径仍从 TaskStore 取得。每次操作由原生对话框确认工作树和实际路径范围；读取信任不会自动执行写入。
- 确认前及取得索引锁后分别校验当前 entry 与请求中的已显示状态。过期分组拒绝写入、不自动重试。暂存明确采用执行时的工作区内容，不声称冻结用户先前看到的 diff 内容。
- 先以 create_new 获取真实 index.lock，再复制已有索引到独立临时目录，通过 GIT_INDEX_FILE 在临时索引中执行 Git 命令。成功后同步锁文件并原子替换真实索引；失败或预算取消在发布前丢弃临时结果。
- 不删除或覆盖其他 Git 进程已有的索引锁；不回滚无关暂存内容。保护针对遵守 Git 锁协议的并发工具，不声称阻止有权任意改写仓库的恶意进程或受信任配置代码。
- 无 HEAD 的取消暂存使用精确索引移除；有 HEAD 时按读取到的提交恢复所选索引路径，均不修改工作区文件。已删除工作区文件使用精确索引移除，避免路径被重新建成目录后递归暂存其内容。
- 项目内已暂存 rename 的取消暂存同时处理源与目标；写入预检额外读取有界仓库级状态以发现跨项目重命名，再过滤到当前项目。普通侧栏读取仍只查项目范围；跨项目 rename 拒绝，并提示从完整仓库项目操作。
- 现有工作区文件通过安全句柄拒绝 reparse point、固定文件及祖先，超过 8 MiB 拒绝暂存；索引读取上限 32 MiB。支持 linked worktree 和 split index；不递归处理子模块内部。
- 索引命令禁用 Git hooks；用户/仓库配置的文件过滤器仍可能运行，原生确认说明该边界。回归先证明 post-index-change hook 会执行，再验证禁用后不执行。
- 冲突须单独选择“标记已解决”并原生确认；普通暂存/取消暂存不接受冲突 entry，不自动判断冲突标记是否已经清理。
- 前端写入控制器拒绝重复点击、不排队和自动重试。成功后刷新项目视图；同一文件的旧差异关闭。失败明确提示结果未确认，随后重新读取状态；切换项目不会覆盖新列表。
- 索引处理期间接入主窗口关闭保护，完成后可重试关闭。进程被外部强杀或系统掉电可能遗留 Git 标准锁，本轮不自动删除或恢复未知锁；显式执行中取消按钮和崩溃恢复仍待补全，原生确认取消已支持。
- 验证：Rust 134 项通过、0 失败、0 跳过，包含 13 项索引事务/操作测试及联网模型目录、运行时隔离安装/验证/回滚、真实 Pi 会话恢复；前端 61 项通过，类型检查零错误零警告，Clippy -D warnings 与桌面构建通过。
- 1280px 浏览器隔离夹具验证真实侧栏组件的暂存后分组、忙状态禁用、失败重试、取消确认不改状态、跨项目结果隔离、长中文路径省略和 28px 操作按钮边界；页面无横向溢出。本切片未重新验收窄窗口，未操作原生 Git 确认弹窗，不以夹具模拟写入冒充原生 IPC 闭环。
- 已生成 src-tauri/target/debug/deeppi.exe；原有 SSR Channel 和 Windows 链接器提示仍存在。未暂存、提交或推送当前开发仓库的改动；上述写入测试只操作隔离临时仓库。

## 14. U4 提交范围与普通提交流程

2026-09-08，已实现普通提交主链，推送及特殊序列流程仍未完成：

- 侧栏新增独立提交面板：提交说明、审阅暂存范围、提交按钮、结果与提交 ID。准备范围前不能提交；准备或提交期间禁用重复操作，并复用主窗口关闭保护。
- 提交说明按项目在当前应用会话内保留，切项目不串稿；取消原生确认不清稿，失败或结果未知不自动重试；只有已确认成功才清除对应草稿。尚未做跨应用重启的草稿持久化。
- 新增 project_git_prepare_commit 与 project_git_commit，仅 main Webview 可调用。原生最终确认包含工作树、目标引用、作者、签名状态、暂存树 ID 和有界摘要；完整路径范围在应用中可滚动审阅。
- 通过临时索引生成 tree OID，准备与执行均核验 HEAD、目标引用、tree、路径集合及身份/签名状态。即使状态字母没变，已暂存内容变化也会拒绝旧请求。提交只采用暂存树，不自动暂存或改写工作区。
- 使用 Git commit-tree 创建指定树的提交对象，再由 update-ref 核验预期旧 OID 并发布引用。实际索引文件字节与未暂存/未跟踪文件保持不变；首次提交、普通分支和分离 HEAD 有回归。
- 普通分支在发布期间固定 HEAD 文件并重核引用，同时用 Git 的旧值校验保护分支。分离 HEAD 不能仅依赖 --no-deref：真实失败测试证明它仍可能覆盖刚变成符号引用的 HEAD。因此先让 Git prepare 引用事务取得锁，再在锁内检查 HEAD 原文，只有仍为预期直接 OID 才 commit，否则 abort。
- 提交路径核验显式包含子模块指针并禁用替换对象解释。先证明 diff.ignoreSubmodules=all 与 refs/replace 能隐藏项目外的实际暂存变化，再修复；发现项目外暂存时要求从完整仓库项目提交，不扩大子目录授权范围。
- 尊重 commit.gpgSign，明确传递签名开关；签名失败不降级成无签名提交。原生确认明确告知本次不运行 Git hooks，也不会推送；用户签名工具成功交互的原生验收仍待进行。
- 发布前支持现有取消令牌和协作式预算；进入引用发布边界后拒绝迟到取消，使用单独有界的命令及收尾等待。异常后核验引用，区分已提交、未提交、结果未知；不会根据一个 IPC/进程错误就自动重复提交。
- 未发布的候选提交可能暂留对象库，交由 Git 回收；不把此类对象当作分支提交成功。强杀、掉电后的操作日志恢复及持久化结果核对尚未实现，不声称覆盖所有崩溃窗口。
- merge、rebase、cherry-pick、revert 和 sequencer 进行中明确拒绝普通提交，并提示先完成对应流程。本轮不伪装成特殊状态已经收口；这些专门流程仍在总目标范围内。
- 浏览器真实提交组件夹具验证：准备期间禁用、完整范围展示、取消保留草稿、未知结果不清稿、跨项目草稿隔离、外部变更使旧范围失效、重新准备后成功清稿。1280px 页面和输入框无横向溢出；未据此声称窄窗口或原生确认/签名验收通过。
- 最终验证：Rust 145 项通过、0 失败、0 跳过（含 11 项提交测试及联网模型目录、运行时隔离安装/验证/回滚、真实 Pi 会话恢复）；前端 67 项通过；类型检查零错误零警告、Clippy -D warnings 和最新桌面构建通过。
- 首次全 Rust 运行达到 200 秒工具总时限，未计为通过。确认无残留测试进程后改用 --test-threads=4 与更长工具时限完整重跑通过，没有关闭或跳过联网用例。原有 SSR Channel、Windows 链接器提示及浏览器工具遥测超时仍如实保留，不混同应用验收结果。
- 已生成 src-tauri/target/debug/deeppi.exe。本轮没有提交或推送当前开发仓库；真实提交测试仅使用隔离临时仓库。
- 下一步：推送远程/目标分支确认与 bare remote 闭环；随后补特殊 Git 状态、执行中取消入口、崩溃恢复和原生交互，再继续 U2/U3 的余项、U5 编辑、U6 设置与整体验收。

## 15. U4 单目的地推送

2026-09-08，推送主链及隔离 bare remote 验证已实现，真实 GitHub 与完整同步体验尚未验收：

- 新增 project_git_push_targets 和 project_git_push，仅 main Webview 可调用；目的地来自 Git 已配置远程及展开后的推送 URL，不接受前端任意新增地址。
- 独立推送面板提供加载远程、明确选择目的地、目标分支、固定源提交 ID 和取消按钮。多个 push URL 分别列为选项，没有默认全选；提交成功不自动推送。
- 原生确认展示工作树、远程、唯一目的地、完整目标引用及源提交；明确传输提交的可达历史，不把子目录项目误称为只会上传子目录。
- 推送前重查源 HEAD、分支与目的地，拒绝过期配置；发送固定 OID:refs/heads/target，不使用易变的 HEAD 作为发送参数。
- 不修改仓库原始配置；使用独占、持有安全句柄的临时配置定义随机远程名。Git remote get-url 不接受本机命令作用域临时远程，实际改为 ls-remote --get-url（不联网）核验展开结果，并核验唯一 pushurl。完成后清理临时配置。
- 隔离原远程 mirror、额外 refspec 和多个地址；显式禁止强推、附带标签、递归子模块、客户端 hooks、隐式 push options、推送证书及 HTTP 重定向。用户和系统的受信任传输/凭据配置仍可能执行代码，不能声称完全沙箱化 Git。
- 地址仅支持无内嵌凭据的 HTTPS、SSH 和本地绝对路径；拒绝 HTTP、UNC、可执行协议、查询参数和片段。错误不回显被拒绝的敏感地址；尚未实现对含凭据地址的脱敏可选展示，当前失败关闭。
- 二次 URL 重写若改变已确认目的地则拒绝；覆盖常见配置扩张与正常并发变化，不声称阻止拥有仓库写权限的恶意进程在预检后任意改写配置。配置快照一致性仍需继续加强。
- 推送使用现有凭据，关闭终端提示和 GCM 交互；应用内认证、SSH 首次信任/登录、需要交互的凭据工具，以及实际 GitHub 往返仍待单独验收。
- 预检沿用 30 秒协作式预算；实际推送单独有 120 秒进程预算及取消令牌，复用 Job 子进程树收尾。原生确认不会被自动关闭，取消后需返回弹窗才能继续收尾，不宣称硬实时截止。
- 解析有界 porcelain 输出，仅接受唯一预期目标，区分已推送、已是最新、拒绝和结果未知。取消/网络错误不等同远程撤销，未知结果不自动重试；应用内远程只读核对入口仍待补齐。
- 推送期间提交/暂存/重复推送互斥，并保持关闭保护；成功或未知结果后清除旧配置快照，重新操作需加载远程。提交与推送共享一个操作区滚动容器，保留下方变更列表空间。
- 当前随机远程没有配置 fetch 映射，也没有自动设置上游，因此推送后原 remote-tracking refs 与 ahead/behind 不一定立即更新。后续需实现受限目标核对/同步，不把仅刷新本地 status 称为已完成远程同步。
- 8 项真实推送回归覆盖：固定源及中文目标、已是最新、非快进拒绝、多地址单选/mirror/refspec/标签隔离、源/地址变化拒绝、敏感地址拒绝、二次重写拒绝、hooks/push options 隔离与原配置不变、运行中取消及进程清理（部分测试覆盖多个断言）。
- 浏览器隔离完整侧栏夹具验证目的地必须选择、长地址换行、写入互斥、成功结果、取消后未知结果和忙状态释放；1280px 页面无横向溢出。修复重复滚动容器后继续验证交互；本轮未完成窄窗口截图和原生弹窗验收，不以模拟端口替代真实 IPC。
- 最终验证：Rust 153 项通过、0 失败、0 跳过，包含联网模型目录、运行时隔离安装/验证/回滚和真实 Pi 会话；前端 72 项通过，类型检查零错误零警告、Clippy -D warnings 与最新桌面构建通过。原有 SSR Channel、Windows 链接器提示和浏览器工具遥测超时仍保留。
- 已生成 src-tauri/target/debug/deeppi.exe。没有暂存、提交或推送当前开发仓库；所有真实推送测试只操作隔离本地 bare remote。

## 16. U4 远程结果只读核对

2026-09-08：

- 新增 main-only 的 project_git_verify_remote 与推送面板“核对远程结果”入口。保留本次实际尝试的目的地、目标分支及源提交；推送取消或通信异常后可手动查询，不自动重推。
- 使用已配置目的地及固定目标执行 ls-remote，只查询远程当前公布的引用，不 fetch、不更新本地引用或 FETCH_HEAD，也不修改上游设置。复用地址重写核验、安全句柄、取消令牌和有界进程预算。
- 查询与原推送结果独立：一致只表示查询时公布的目标 OID 一致；不同不能推断原推送未发生，也不判断祖先关系；空结果显示“查询未返回目标分支”，不排除服务端可见性限制。连接失败、输出超限或格式异常不当作目标缺失。
- 核对不要求当前本地 HEAD 仍是原提交；远程配置已移除或目的地变化时拒绝用新地址核对旧推送。响应要求唯一、完整、合法 OID 与精确目标引用。
- 先用失败测试复现本地刷新误丢弃历史远程核对响应，再分离项目切换和本地刷新代次。切项目后再切回来仍忽略旧响应，取消核对后明确提示状态未确认；相同项目往返隔离也应用于推送响应。
- 前端 79 项测试通过，类型检查零错误零警告，Clippy -D warnings 通过。完整 Rust 运行 156 项，首次 155 通过、1 项联网模型目录全局超时、0 跳过；失败用例随后原样单独重跑通过。真实运行时联网安装/回滚与 Pi 会话测试在首次完整运行中通过，未禁用联网测试或改宽断言。
- 浏览器隔离侧栏夹具验证取消推送后核对、一致/不同/未返回/查询失败四种显示、保留原未知结果、推送次数始终为 1、忙状态释放与切项目清理。1280px 页面无横向溢出，操作按钮高度 28px；未重新验收窄窗口或原生 IPC，不以模拟端口当作实际 GitHub 验收。
- 最新桌面构建通过，生成 src-tauri/target/debug/deeppi.exe，包含第 12 节关闭窗口权限及防重入修复。原有 SSR Channel 未使用、Windows 链接器提示仍存在；未接管原生窗口，实际关闭/最小化仍待验收，运行中的旧程序需要重启新版才加载新权限。
- 待续：受限跟踪引用同步、特殊 Git 状态、持久化操作恢复、原生认证/确认及真实 GitHub 验收；U2/U3 余项、U5 编辑、U6 设置和整体验收仍未完成。没有暂存、提交或推送当前开发仓库。

## 17. U4 显式远程跟踪同步

2026-09-08：

- 新增 main-only 的 project_git_sync_tracking；推送尝试记录下提供独立“同步跟踪引用”按钮，不在提交或推送后自动执行，不再次推送。同步对象来自此记录的原目的地和目标分支。
- 读取该远程实际拉取地址及 fetch refspec。仅当首个拉取地址与所选推送目的地一致时同步；支持精确、单通配和负 refspec、多条映射去重。只接受 refs/remotes/ 目的引用，拒绝从该入口改写本地分支或标签，不盲写 origin/main。
- 原生确认列出工作树、远程、目的地、查询提交及全部待更新跟踪引用的旧 OID；明确允许跟踪远程回退，不合并、不变基、不设置上游。确认返回后重核远程提交、映射和本地旧值，变化则拒绝旧操作。
- 先以完整 OID 获取可达对象，命令行清空 refmap、禁止写 FETCH_HEAD、自动维护、提交图写入、标签、剪枝、递归子模块及客户端 hooks；不指定下载目的引用。原始远程配置不变，继续复用第 15 节临时配置和目的地重写核验。
- 获取完成后核验对象类型为 commit，再通过 update-ref 的 prepare/commit 事务发布所选跟踪引用。旧 OID 比较保护并发修改，prepare 后锁内再次拒绝符号引用及映射变化；测试覆盖符号引用恰好解析为旧 OID 的竞态，不把 --no-deref 当作类型检查。
- 现有引用锁不会被删除或覆盖；任一准备失败时中止所有排队更新。正常事务成功不等于多引用对并发读者具有瞬时一致性，也不声称覆盖强杀/掉电后的所有崩溃窗口。
- 同步操作的协作预算为 180 秒，包含排队及原生确认等待；获取进程最长 120 秒且不超过剩余预算。进入发布边界前可取消，进入后等待有界发布结果。原生弹窗不会自动消失，阻塞系统调用不保证硬实时截止。
- 远程查询无结果时不删除本地引用；获取失败或取消在发布前不更新引用，已经下载的对象可能保留。引用发布后异常明确返回 unknown，不自动重试；操作恢复和持久化结果核对仍待补。
- 前端同步与提交、暂存、推送及核对互斥，复用关闭保护。成功、未知或通信异常后刷新本地状态；取消原生确认不改变原推送记录。跨项目及切走再切回不会回写旧结果，迟到取消不会掩盖已完成的同步。
- 9 项真实同步回归通过：映射/排除、工作区和索引/FETCH_HEAD 保留、不同拉取目的地拒绝、远程新增对象下载及多映射更新、标签/剪枝/hooks 隔离、并发旧值变化、锁冲突、符号引用竞态、取消/缺失目标、linked worktree 共享引用与两套 HEAD 保留（部分测试包含多项断言）。
- 浏览器完整侧栏夹具验证取消确认、成功刷新领先计数、未知与失败提示、原推送记录保留，推送次数始终为 1。1280px 页面无横向溢出，操作按钮高度 28px；同步执行中的取消由控制器和后端取消边界测试覆盖，本轮没有进行原生确认、真实 GitHub、窄窗口或截图验收。
- 最终验证：Rust 165 项通过、0 失败、0 跳过，含联网模型目录、运行时隔离安装/回滚及真实 Pi 会话；前端 85 项通过，类型检查零错误零警告、Clippy -D warnings、桌面构建通过。构建仍保留原有 SSR Channel 未使用和 Windows 链接器提示。
- 最新程序为 src-tauri/target/debug/deeppi.exe。当前开发仓库没有暂存、提交或推送；所有真实 Git 写入测试仅使用隔离临时仓库和本地 bare remote。
- 待续：特殊 Git 状态专门流程、持久化操作恢复、认证及原生/真实远程验收；上游设置与完整 fetch/pull 管理仍未实现。U2/U3 余项、U5 编辑、U6 设置和长期运行验收仍在总目标内，整个重设计未完成。

## 18. U6 六类设置布局与保存时序

2026-09-08，六类设置布局已实施，U6 整体验收仍未完成：

- 设置页改为通用、外观、模型与凭据、运行时与更新、Pi 扩展、高级与诊断六类常驻导航。窄窗口改为多列分类按钮，支持箭头、Home/End 导航与输入法组合输入保护；仍无 Ctrl+P 浮层。
- 顶层设置菜单增加外观、运行时、高级分类直达；帮助中的应用更新直接进入运行时分类，文件预览的编辑器配置直接进入高级分类。Ctrl+, 和工作区设置按钮保留当前分类。
- 模型与扩展使用原真实组件嵌入，不再跳离统一设置导航。首次访问后，在同一次设置页会话内保留挂载，分类往返保留模型表单和扩展搜索状态；退出设置后仍按既有生命周期卸载，不宣称跨应用重启的草稿持久化。
- 拆出 RuntimeSettings 和共享设置控件样式，保留应用更新、组件检查/更新/修复/回滚/稍后/跳过及取消回调。更新与凭据后端、持久化字段、DSH Webview 均未迁移或开放新权限。
- 通用分类增加已有 maxConcurrentTasks 字段的编辑入口，输入时校验 1 至 16 的整数；无效或不完整值不发送保存。外观使用现有语义颜色与字体变量；模型及扩展补充基于实际容器宽度的布局规则。
- 修复 Provider 顶部保存按钮只引用 saveProvider、没有调用函数的问题。隔离夹具通过注入命令端口验证真实按钮发出一次 save_pi_provider 调用；不访问真实凭据，不把模拟保存计作原生 Credential Manager 验收。
- 应用设置保存改为单请求串行、合并待发送完整快照，复制可变更新偏好，避免本窗口快速修改时旧请求后完成覆盖新值。失败明确报告，不进行后台无限重试；显式 flush 可重试当前未保存值一次。
- 主窗口 destroy 前等待设置保存队列；单元测试覆盖保存完成前不销毁窗口。扩展操作期间禁用设置返回，并保护主要离开设置入口；扩展/运行时操作也接入主窗口关闭保护。原生关闭、取消及错误弹窗往返仍待单独验收。
- 高级分类复用外部编辑器配置，展示实际读取的运行时版本/来源/可用状态及已有更新错误。不新增虚构日志数据；脱敏诊断日志、导出、完整性能与长期运行诊断仍待实现。
- 前端 94 项测试通过，类型检查零错误零警告；生产网页及桌面构建通过，最新程序为 src-tauri/target/debug/deeppi.exe。本轮未修改 Rust 后端，未重新运行联网 Rust 测试，不将上一轮 165 项通过重复计作本轮结果。
- 浏览器夹具检查了 320px、390px、768px、1440px 的相关分类截图与控件边界，包含浅色外观、深色扩展/高级设置、模型表单布局；检查的页面无横向溢出。验证了范围错误提示、分类箭头导航、模型草稿保留与保存调用、扩展搜索保留、运行时回滚回调。
- 隔离 Vite 测试服务补上 $lib 别名后重新加载真实扩展组件；旧占位片段不计作扩展页验收。原 1420 开发服务曾显示与当前源码不符的旧 Git 编译错误，未修改已正确的 Git 文件，也未停止原服务；新增 1422 本机生产预览用于正式页面验证。
- 1422 正式预览验证设置菜单直达运行时分类，页面无 JS 错误；1024px 截图发现工作区两行网格使设置只占上半区。已增加仅设置视图生效的单行网格，并让夹具使用相同主区类名；该最终高度修复已通过检查、测试和重建，修复后的正式页截图仍需下一轮复验。
- 预览地址为 <http://127.0.0.1:1422/>，隔离夹具仍为 1421。临时浏览器视口已恢复，测试标签已清理。构建原有 SSR Channel 未使用和 Windows 链接器提示仍保留。
- 未暂存、提交或推送当前开发仓库。后续继续最终高度复验、设置原生交互与诊断，再推进特殊 Git 流程/恢复、U2/U3 余项、U5 编辑以及 U6 完整验收；总目标保持未完成。

## 19. 关闭顺序补强、设置高度复验与 U5 版本基础

2026-09-08 至 2026-09-09（本地时间）：

- 保留第 12 节主窗口 destroy/minimize 权限和关闭防重入修复。新增可取消的 prepareExit 阶段，在停止 Pi/DSH 前等待设置保存队列；保存失败或取消时保持任务和窗口，随后可再次关闭。异步准备结束后重新检查 Git、运行时和扩展等忙状态，最小化不执行退出准备。
- 先以失败测试复现准备未执行导致任务提前停止，再修正关闭控制器和正式页面接线。设置保存集成测试同步改为验证队列完成前既不停止任务，也不销毁窗口；未以浏览器测试替代原生权限、关闭按钮及弹窗往返验收。
- U5 的 read_project_file 返回不透明 version；SHA-256 同时包含相对路径、祖先及最终文件的卷/文件身份与原始内容字节。前后端 FilePreview 契约同步，既有 2 MiB、UTF-8、NUL、路径和 reparse point 限制保持。
- 4 项新增真实文件回归覆盖版本稳定、同长度内容与换行变化、同内容新文件替换，以及 hard link 路径和祖先目录替换。原始 BOM、混合换行与字节数不被读取过程改写；版本不是写入锁，也不能单独提供原子条件保存。
- 先前 project_edit 的 SetFileInformationByHandle POSIX rename 实验在当前 Windows 上报共享冲突，无法替换未共享 DELETE 的受保护目标。已移除未接入产品的失败实验及模块注册，不将其记为保存功能通过，也不以释放保护后盲目覆盖作为替代。备份保全与竞态检测的替换方案仍须单独验证。
- CodeMirror 依赖已安装，但尚未接入组件；文件界面仍保持只读预览和外部编辑器。多文件草稿、脏状态、冲突比较、保存/另存及关闭确认尚未完成，未开放新的文件写入 IPC。
- 旧 1422 预览仍提供旧页面；独立启动 1423 新版生产预览后，设置视图类名和单行网格生效。正式运行时设置页在 1280x720 和应用最小窗口 900x600 下均与 main 等高、底边一致，无横向溢出，截图已复验。没有修改 DSH Web UI 或扩大其权限。
- 最终验证：前端 98 项通过；Rust 169 项通过、0 失败、0 跳过，包括联网模型目录、运行时隔离安装/回滚及真实 Pi 会话。Svelte/TypeScript 检查零错误零警告、Clippy -D warnings、网页和桌面构建通过；原有 SSR Channel 未使用与 Windows 链接器提示仍存在。
- 最新程序为 src-tauri/target/debug/deeppi.exe。没有接管或自动关闭原生窗口，正在运行的旧实例不会热加载新版原生权限；实际关闭/最小化仍需新版实例验收。
- 新版网页预览为 <http://127.0.0.1:1423/>；浏览器测试标签已清理、视口已恢复。未暂存、提交或推送当前开发仓库；Git 写入回归仍只使用隔离临时仓库及本地 bare remote。
- 后续仍需完成 U5 安全保存及编辑器闭环、特殊 Git 流程与恢复、U2/U3 余项、设置原生交互和诊断、真实远程认证及长期运行验收。总目标保持 active，不以本轮通过的基础测试宣称整个 UI 重设计完成。

## 20. U5 内置编辑与版本保存主链

2026-09-09，本轮实现主链，尚未完成 U5 界面及原生验收：

- 新增 main-only 的 save_project_file，项目根从 TaskStore 取得。共享保存 gate 使用 try_lock 拒绝同时写入，不把多个写入请求排队；保留项目相对路径、UTF-8/NUL/2 MiB 和祖先 reparse point 拒绝边界。
- 保存先比对 expectedVersion。同目录独占临时文件写入并 sync 后，已有文件使用 ReplaceFileW 保留实际被替换的旧文件；另存使用不带 REPLACE_EXISTING 的 MoveFileExW，迟到创建者不会被覆盖。
- 普通保存跨替换边界继续持有原文件拒绝写入、允许删除的只读句柄，并固定父目录及祖先。外部工具仍可能以替换方式换入新文件，因此不宣称这是 Windows 原子条件 CAS。
- 替换后核验实际旧文件的版本，并核验目标身份和内容等于本次候选。正常旧值变化返回 conflict，API 部分失败、临时内容变化、发布后核验失败返回 unknown；未知结果不自动重试。竞态时目标可能已经是本次内容，界面明确提示并保留恢复路径。
- 成功替换也保留同目录随机 .deeppi-*.recovery 旧版本副本，避免清理过程再发生路径竞态。当前没有自动清理、持久化恢复目录或副本管理入口，副本会累积，须继续实现管理体验；未知结果返回的 pending/recovery 路径是待核对路径，不保证每个都实际存在。
- 原子替换不等于强杀/断电恢复事务；临时文件发布前后仍有需扩展的崩溃恢复边界。拒绝初始 reparse point 并固定祖先，不将当前并发测试宣称为对拥有目录写权限的恶意进程的完整沙箱证明。
- 11 项真实保存回归涵盖 UTF-8/BOM/混合换行字节、版本冲突、外部替换保全、目标删除、只读拒绝、目录联接拒绝、路径/大小/二进制验证、父目录锁定、另存迟到创建和临时内容变化。先观察失败再实现，不保留上一轮失败的 POSIX rename 假设。
- 接入 CodeMirror 6，按需加载编辑器核心、状态与语言。主包曾增长到 522.63 kB，拆分后应用主块 217.71 kB、编辑器共享块 304.33 kB；500 kB 构建警告消除。此为构建体积，不是首屏网络或运行性能测量。
- 文件区域新增标签页、脏标记、保存、另存、重载、并排内容比较、外部编辑器和返回任务入口。Ctrl+S/Ctrl+Shift+S 与文件菜单接入；保存组合键保护 IME composition，Ctrl+P 仍只聚焦侧栏文件搜索，不新增浮层。
- 草稿由页面级控制器持有，切任务、项目、Git diff、设置不会丢弃文档状态；最多同时打开 32 个文件。保存只将实际提交的快照设为基线，保存期间继续输入的内容仍为未保存；自动刷新保留草稿并报告磁盘版本变化。
- 关闭标签、退出窗口和移除项目使用统一草稿选择。取消和未确认保存保留草稿；选择放弃退出时不会提前清空内存，后续任务停止失败仍可返回编辑。移除项目的后端等待期锁定文档，失败解锁并保留草稿。
- 13 项控制器回归覆盖多文件/跨项目状态、换行保留、保存中输入、刷新中输入、未知结果不自动重试、另存重定位/目标预约、取消关闭、关闭确认期间输入变化、迟到读取和项目移除失败解锁。
- 混合换行通过固定 LF 分隔保留 CR 原字符，读取与编辑序列化测试不改写既有字节；单独 CR 在混合文档内的视觉行号、复制粘贴和原生 IME 仍需界面验证。并排内容比较尚无差异高亮或合并操作；多文件状态当前仅在进程内保留，不是崩溃草稿恢复。
- 新增真实 FileEditor/CodeEditor/AppDialog 组件夹具，提供延迟保存、IPC 失败、外部修改和主题切换端口。访问本机 1421 测试页面被浏览器安全策略明确拒绝，未换端口、换浏览器或通过桌面自动化绕过。不得据单元测试或构建声称截图、IME、主题对比度、交互状态及 Tauri 保存闭环验收通过。
- 最终验证：前端 111 项通过；Rust 180 项通过、0 失败、0 跳过，包括联网模型目录、运行时隔离安装/回滚及真实 Pi 会话；类型检查零错误零警告、Clippy -D warnings 和桌面构建通过。原有 SSR Channel 未使用和 Windows 链接器提示仍存在。
- 已生成 src-tauri/target/debug/deeppi.exe，未接管原生窗口，未暂存、提交或推送当前开发仓库。既有正式预览为 1423，编辑器隔离夹具为 1421/editor.html；本轮未启动新服务绕过浏览器拒绝。
- 后续优先恢复获准的浏览器验收，补齐恢复副本管理、编辑器 diff/主题/IME/包体积和原生交互验证，再继续 U2/U3 余项、特殊 Git 流程/恢复、真实认证、诊断与长期运行。完整目标保持 active，U5 不标为整体完成。

## 21. U5 退出期间草稿锁定与读取代次隔离

2026-09-09（本地时间）：

- 先以失败测试复现：确认退出之后、后台停止任务与销毁窗口期间，文件控制器仍接受新输入；另外关闭后立即重开同一路径会复用读取代次，旧实例响应可能覆盖新文档。
- 新增 prepareExit 生命周期锁：准备时冻结现有文档，阻止新开文件、普通保存、关闭标签和项目移除；用户选择保存时，仅退出内部流程可保存冻结快照。保存失败、取消或对话异常会释放锁并保留文档。
- 窗口关闭控制器接受退出准备返回的释放函数，锁覆盖 Pi/DSH 停止和窗口销毁，并在 finally 中释放。重复关闭不会重复取得或释放锁；其他操作在准备后进入忙状态时，中止退出也会释放锁。
- 文档退出锁与实际保存/项目移除 busy 状态分离，避免把退出自身锁定误判成仍有文件操作，从而重新引入“确认后无法关闭”。页面停止阶段使用 inert 和快捷键保护暂停交互，DSH 子 Webview 的可见性同时受退出状态限制；失败后恢复。
- 文档读取使用控制器级单调递增的请求代次，不因同路径重开而重置。旧成功及错误响应不会回写新的同路径文档；关闭确认也检查读取代次。取消退出会重新启动被锁定过程打断的首次读取。
- 新增 9 项回归，覆盖锁定期间输入与打开/保存/关闭拒绝、锁内保存、取消/异常解锁、重复退出锁所有权、同路径重开旧读取隔离、首次读取恢复、真实控制器与关闭处理器集成，以及停止/销毁/准备后忙状态失败的释放。
- 完整前端 120 项通过；Svelte/TypeScript 零错误零警告，网页与桌面构建成功，更新 src-tauri/target/debug/deeppi.exe。本轮未改 Rust，未重新执行 Rust/联网测试，不将第 20 节 180 项重复计作本轮结果。
- 浏览器权限拒绝尚未解除，本轮未重试或绕过访问限制，也未接管原生窗口。退出时的实际焦点、DSH 显隐、编辑器交互和原生关闭仍需验收；单元集成测试不替代这些证据。
- 恢复副本管理、编辑器差异高亮/合并、主题/IME 与崩溃草稿恢复仍未完成，其他 U2/U3/U4/U6 余项保持原范围。未暂存、提交或推送当前开发仓库，总目标保持 active。

## 22. U5 只读文件差异视图

2026-09-09（本地时间），差异显示已接入，界面和原生验收仍未完成：

- 安装 @codemirror/merge 6.12.2，使用成熟 MergeView/Chunk 差异实现替换纯文本并排比较。差异组件独立按需加载，不加入未打开文件时的主工作区加载路径。
- 左侧为所选磁盘来源，右侧为发起比较时的草稿快照。逐字/逐行差异高亮、行号、差异数量及上一个/下一个差异按钮已接入；编辑器内 F7/Shift+F7 导航，不绑定普通非交互 section 的键盘事件。
- 两侧同时设置 readOnly、不可编辑内容区域和拒绝文档修改的 transactionFilter，不启用 revertControls，不通过比较组件调用保存接口。比较不修改原草稿、撤销栈或磁盘内容；关闭比较后恢复原编辑区并请求重新测量。
- 来源菜单区分当前磁盘文件、另存时的上次目标、旧版本恢复副本和待写内容副本。只展示当前文档记录中存在的来源；恢复副本读取失败明确报错，不自动回退到其他文件。副本持久化目录、跨重启发现和清理仍未实现。
- 比较请求使用独立单调代次；切换文件、重新比较、切换来源、关闭或卸载后，旧成功/失败响应不回写。响应路径须与请求匹配，并继续限制 UTF-8 文本、NUL 和 2 MiB 大小。
- 草稿与读取来源均为快照。后续文档内容、基线或已观察到的当前磁盘版本变化会标记快照过期；未实施持续文件监听，不把快照称为实时磁盘状态。草稿相等性复用 CodeMirror Text，避免光标移动时反复序列化整个文档。
- 为行对齐，仅比较用副本将换行归一化为 LF；原始草稿与读取内容不改写。界面单独显示 LF/CRLF/CR/混合及 UTF-8 BOM 元数据，纯换行变化明确提示，不能被“内容相同”掩盖。BOM 和空白变化仍进入真实差异计算。
- diffConfig 设置 scanLimit 500、timeout 100 ms；算法退化为较粗差异时按 Chunk.precise 明确提示。该预算只针对算法的协作式检查，不宣称覆盖字符串处理、DOM 排版或整个操作的硬时限。
- 新增 13 项测试：7 项比较来源/快照/过期响应/错误/大小校验，6 项真实差异配置与引擎测试，包含只读修改拦截、新增/删除/Unicode、BOM/空白/换行、差异导航和 2 万行重复文档保留。此大文档用例为逻辑回归，不是浏览器性能实测。
- 完整前端 133 项通过；Svelte/TypeScript 零错误零警告，许可证检查通过，pnpm audit --prod --audit-level high 未发现已知漏洞。网页和桌面构建成功，更新 src-tauri/target/debug/deeppi.exe；原有 SSR Channel 未使用和 Windows 链接器提示仍存在。
- 本轮未修改 Rust，未重新执行 Rust 联网测试。浏览器访问拒绝仍有效，未重试或绕过，也未接管原生窗口；截图、深浅主题对比度、窄窗口、滚动对齐、键盘/IME 及真实 Tauri 文件比较仍须验收。
- 未实现差异合并/逐块采用，也不把只读高亮当作合并功能。恢复副本管理、崩溃草稿恢复及 U2/U3/U4/U6 余项保持原范围；未暂存、提交或推送当前开发仓库，总目标保持 active。

## 23. U5 恢复副本持久登记与管理入口

2026-09-09（本地时间），恢复管理主链已接入，原生和界面验收仍待完成：

- 保留第 12、19、21 节窗口关闭权限、防重入及退出锁修复，单独复验 15 项关闭测试通过。新版构建包含这些修复；没有接管运行中的旧窗口，未将实际关闭按钮和原生弹窗往返标记为已验收。
- 新增独立 SQLite file-recovery.db，位于应用 roaming 数据目录；WAL 与 FULL 同步策略。保存先持久登记项目、目标、随机副本路径及父目录/全部祖先身份，再创建临时文件和发布；登记失败不写入工作文件。记录不存源码正文。
- 只管理本版本登记的操作意图，不扫描或自动认领历史 .deeppi-* 文件。登记的 pending/recovery 不保证均已生成，发布后 pending 常不存在；缺失记录可显式移除，未实施自动修剪或历史副本导入。
- main-only 的列表、版本化读取、删除与另存恢复接口已注册。列表每页 25 条、全库最多 20000 条；文件状态区分可用、缺失、目录变化、无法读取。目录身份变化时不访问该记录对应的副本。
- 删除要求登记所属项目和显示时版本匹配，通过拒绝写入/删除共享的同一 DELETE 句柄核验并标记删除，不释放保护后再按路径删除。仅移除记录使用独立语义，不删除或访问副本文件；两种操作都经过明确的原生确认。
- 恢复只创建新文件，不覆盖已有目标，保留原副本；最终发布复用既有 create-only 保存路径。恢复与普通保存共用后端互斥锁，覆盖原生确认等待期。
- 文件菜单增加独立“恢复副本”入口，无需打开编辑器即可管理。面板按需加载，显示来源、实际路径、时间、状态，提供预览、与磁盘快照比较、另存恢复、删除副本和仅移除记录；比较目标读取失败明确报错，不伪造空文件。
- 复用只读差异组件，右侧明确标为“磁盘快照”，不冒充草稿；预览/比较关闭和迟到读取隔离。面板保留固定项目身份，切导航不卸载正在进行的写入；恢复输入及原生确认期间保护窗口关闭和项目移除。
- 前端写操作传递显示时版本，取消不清空列表，通信错误不自动重试。跨项目写互斥、列表请求代次和旧记录拒绝已测试；先复现分页期间新增记录导致重复行，再按记录 ID 去重。分页不是跨请求一致性快照，并发新增/移除后仍需刷新核对。
- 10 项真实后端恢复测试通过，覆盖持久重开、不认领未登记文件、版本/项目隔离、精确删除、根和嵌套目录替换、记录移除不误删、路径验证、登记失败不写入、create-only 恢复和分页。独立只读核验未发现范围内误删/覆盖缺陷，数据库清理失败的专门故障注入仍待补。
- 最终前端 141 项通过；Rust 190 项通过、0 失败、0 跳过，包括联网模型目录、隔离运行时安装/回滚及真实 Pi 会话。Svelte/TypeScript 零错误零警告；后端实现的 Clippy -D warnings 已通过，之后未修改 Rust。
- 网页和桌面构建成功，更新 src-tauri/target/debug/deeppi.exe。原有 SSR Channel 未使用及 Windows 链接器提示仍存在。浏览器访问拒绝尚未解除，未重试、换端口或接管桌面绕过；截图、主题、焦点、原生确认/关闭与真实 Tauri 恢复闭环仍未验收。
- 文本预览与版本化删除继续限制 UTF-8、无 NUL、最大 2 MiB；无法读取的副本可仅移除记录或外部处理。持久意图不等于崩溃草稿恢复或掉电原子事务，不声称防御拥有同一目录写权限的任意恶意进程。
- 没有暂存、提交或推送当前开发仓库。差异合并、历史副本导入、崩溃草稿、特殊 Git 流程/持久恢复、真实认证、诊断及 U2/U3/U6 余项保持原范围，整个重设计目标仍为 active。

## 24. U2 结构化消息显示与历史阅读

2026-09-09（本地时间），显示层主链已接入，U2 整体仍未验收完成：

- 正式 ChatPane 按需加载 ChatMessage，正文支持标题、段落、强调、引用、嵌套列表、表格、链接、行内代码和代码围栏。用户输入保持字面文本，不把粘贴的 Markdown 自动解释成助手内容。
- 思考、工具参数、最终工具输出、图片和未知内容分开呈现，折叠区展开后才挂载内容。未知消息块保留可查看的文本，不静默丢弃；格式化模块失败时回退完整文本并提供重载入口。
- 代码块提供语言标识、复制和换行开关；消息提供文本复制与原始文本切换。复制调用系统剪贴板接口，失败明确报错；未自动创建文件，也不把剪贴板调用接线等同于原生复制验收。
- 初次引入 markdown-it/highlight.js 时，许可证检查发现 PSF-2.0、BSD-2-Clause、BSD-3-Clause 不在现有清单。随后移除这两项依赖，改用 marked 18.0.12 与 he 1.2.0，并复用 CodeMirror/Lezer 的语法和固定高亮类。未修改或放宽 scripts/check-licenses.mjs。
- Markdown 仅使用解析器 token，经固定类型/标签映射成为 Svelte 组件树；HTML token 作为字面文本显示。正文不使用任意 HTML 注入，也不执行 Markdown 内脚本或 SVG。
- 只有代码高亮生成的固定 span 标签进入 HTML 渲染；每个代码片段均先转义，类名来自 Lezer 的固定 classHighlighter。未知语言、加载/解析失败或超过 64 Ki 个 UTF-16 码元时保留转义后的完整纯文本；不自动猜测语言。
- 高亮等待 150 ms 输入停顿并忽略旧异步结果，复用既有按需语法模块。Markdown 源超过 256 Ki 个 UTF-16 码元，或转换超过 10000 个 token/32 层嵌套时回退完整纯文本，不截断原消息；限制不等于整个解析及 DOM 排版的硬实时预算。
- 代码块内容采用 Markdown 解析器的文本语义，包括围栏与换行规范化；原消息文本仍保留，可通过原始文本/消息复制取得。该显示层不调用文件保存，不改变编辑器的编码、换行或版本保存契约。
- 外链只允许显式 HTTP(S) 且无 URL 凭据；拒绝空白/控制字符、可执行协议和相对本地路径。标准 URL 规范化后显示实际目标，确认后通过现有 Tauri opener 打开；取消、所属会话切换或结束不再执行旧确认结果。
- Markdown 远程图片仅提供显式外链，不自动发起远程图片请求。原生消息中符合大小/格式约束的 PNG/JPEG/GIF/WebP base64 图片仅在展开时挂载；SVG、远程伪装 data 和超限图片明确显示不支持。
- 历史展开仍以每次增加 100 条显示范围为单位，保留首个可见消息的相对位置，不依据整份对话高度变化盲目补偿。阅读历史期间固定起始消息，新消息到来不再把正在阅读的消息挤出显示范围；新增回到最新消息按钮。
- ResizeObserver 合并跟随底部时的内容尺寸变化，包括格式化模块/代码块和折叠内容展开；隐藏视图或用户阅读历史时不主动滚到底部。相关位置算法有单元测试，真实布局、焦点和滚动行为尚未进行浏览器验收。
- 本轮新增 22 项回归，覆盖格式化与实体、原始 HTML/代码转义、链接混淆与确认生命周期、嵌套代码/表格组件静态渲染、折叠思考、结构化消息/图片边界、多语言高亮字符保全、旧消息阅读窗口及展开锚点。
- 完整前端 163 项通过；Svelte/TypeScript 零错误零警告；现有许可证检查通过，最终生产依赖 audit 未发现已知漏洞。网页及桌面构建通过，更新 src-tauri/target/debug/deeppi.exe。原有 SSR Channel 未使用和 Windows 链接器提示仍存在。
- 本轮未修改 Rust，没有重新执行 Rust/联网集成测试；第 23 节 190 项全量含联网通过仍是最近后端结果，不重复计作本轮执行。依赖 audit 本轮实际联网执行。
- 浏览器安全拒绝仍有效，未访问测试页面或通过其他端口/桌面自动化绕过。静态组件渲染不是浏览器挂载、剪贴板、原生外链、主题对比度、真实滚动或 IME 验收。没有暂存、提交或推送当前开发仓库。
- 全量历史当前仍由 Pi get_messages 一次读取，尚未完成后端历史分页、长期内存/性能验收、脱敏诊断及 U2 原生双向模式切换；U3/U4/U5/U6 其他余项保持原范围。整个重设计目标继续 active，不以本轮显示层完成替代整体交付。

## 25. U2 有界历史快照与真实分页

2026-09-09（本地时间），替代第 24 节中“尚未完成后端历史分页”的状态，U2 整体仍待验收：

- 已核对本机 Pi 官方 RPC 实现：get_messages 返回当前 session.messages，原生协议没有分页参数。宿主仍一次接收 Pi 序列化的全部当前上下文，再建立匿名临时文件快照、按页读取；不是原生协议分页，也不代表读取了压缩前或放弃分支的所有归档消息。
- 普通 JSON frame 保持 8 MiB 上限，更大的历史 frame 流式落入匿名临时文件，原始整帧上限 128 MiB。结构化解析逐条验证消息，单条读取预算及序列化内容分别限制 8 MiB，快照最多 100000 条、消息存储最多 128 MiB。
- 每页最多 100 条，消息字节目标上限 2 MiB；允许较大的单条消息独占一页，最大 8 MiB。元数据和数组分隔不包含在消息字节目标内。
- main-only 的 rpc_history_open/page/close 返回不可变 snapshotId、事件游标及 [start,end) 范围，绑定 taskId/runId。注册表最多 64 份、512 MiB 消息存储；该预算不包含正在接收的原始临时 frame、解析中的快照和临时内存。
- 同一运行只允许一个在途历史请求；前端合并重复初始化和翻页。任务结束后已有快照仍可读取；切换、重连和卸载按确切 ID 释放，旧关闭请求不删除新快照。迟到初始化返回时也释放其快照。
- 闲置 30 分钟的快照在后续 insert/page 时惰性清理，不是后台即时删除；page 更新访问时间，过期后明确要求重新打开任务。进程退出释放全部匿名文件，未建立第二份持久原生会话库。
- 已复现并修复超时后迟到的大历史响应导致后续 RPC 断开：按官方已核实的响应字段顺序，以最多 64 KiB 的结构化头部探测提取请求 ID，落盘前识别失效请求并排空其余 frame；已落盘响应解析前再次检查。字段顺序无法预探测时回退完整有界解析，不通过字符串搜索猜测 ID。
- 若请求在完整解析已经开始后才超时，当前仍可能完成本次有界处理；没有中途取消或整个读取流程的硬实时时限。原始 frame 总量限制在丢弃路径仍生效。
- ChatPane 先加载尾页，以初始事件游标接续实时事件；继续向前阅读才获取前页。消息使用绝对索引保留已有组件与阅读锚点，翻页返回时尊重用户后来点击“回到最新”的意图。重新打开快照不复用旧快照局部索引。
- 前端拒绝不连续范围、错误快照身份和变化的游标；失败不推进分页位置，卸载后的迟到结果不回写。prepend 保留实时执行、工具和队列状态，不以旧快照覆盖当前状态。
- 真实 Pi 集成测试用官方 SessionManager 在隔离临时目录建立超过 8 MiB、151 条消息的原生会话，验证所有页面顺序、后续 get_state 和会话文件字节不变；不发送模型 prompt、不读取用户会话或凭据。另以真实 Node 子进程验证大 frame 后事件继续、超时迟到响应排空及后续命令可用。
- 最终 Rust 202 项通过、0 失败、0 跳过，显式执行 --include-ignored，包含联网模型目录、隔离运行时安装/验证/回滚、真实 Pi 工具和恢复测试。前端 171 项通过，包含既有窗口关闭回归；Svelte/TypeScript 零错误零警告，Clippy -D warnings 和许可证检查通过。
- 网页及桌面构建成功，更新 src-tauri/target/debug/deeppi.exe，包含窗口关闭权限、防重入和退出草稿锁修复。原有 SSR Channel 未使用和 Windows 链接器提示仍存在。
- 未访问被安全策略拒绝的浏览器页面、未换端口或接管桌面绕过。原生关闭/最小化、真实长历史滚动、主题/IME、大仓库性能和 8 小时长稳仍未验收。
- 未暂存、提交或推送当前开发仓库。诊断、特殊 Git 流程与持久恢复、真实远程认证、崩溃草稿恢复及 U3/U5/U6 其他余项保持原范围，总目标继续 active。

## 26. U2/U6 Pi RPC 安全诊断查看与导出

2026-09-09（本地时间），新增 Pi RPC 诊断入口，完整诊断和原生验收仍未完成：

- “设置 → 高级与诊断”增加 Pi RPC 诊断区，提供手动刷新、运行批次过滤、事件列表、JSON 预览、确认清空和原生文件导出。诊断首次访问时读取，最多展示 256 条，不增加轮询或自动上传。
- 以应用级有界结构化记录替代原 RPC stderr 原文尾部缓存。记录只接受固定枚举和数值，不接受任意错误字符串、命令内容、提示词、工具参数、路径或凭据；stderr 持续排空，仅累计字节数。
- 批次使用应用进程内递增编号，不导出 TaskStore ID、原生会话 ID 或任务名；包含固定应用版本、系统/架构和相对应用启动时间。任务进程退出后记录仍保留，应用退出不持久保存。相邻同批次同类记录合并计数，超过 256 条移除最早项并显示数量。
- 覆盖进程启动/退出、停止请求/等待超时、输入写入/队列失败、命令失败、请求超时、事件回放缺口、输出协议及 stderr 读取事件。请求等待诊断不记录命令正文；没有将 stderr 字节计数冒充可阅读错误日志。
- 独立只读核验指出错误分类过于笼统后，补充程序/工作目录缺失、权限拒绝、进程树托管及恢复失败分类。共享 process_runner 新增内部结构化启动错误，原 spawn_owned 保留原字符串返回契约与进程收尾逻辑，不解析本地化操作系统错误文本。
- 输出增加非法 JSON、末帧不完整、帧超限、输出读取失败、历史解析/临时存储失败等固定类别。未识别的输出错误仍使用通用类别，不导出任意原始文本。具体业务命令失败原因仍需在原任务中查看，不宣称报告可恢复所有错误细节。
- diagnostics_snapshot/clear/export 均 main-only。导出只接受后端快照 ID，不接受前端提供的正文或任意路径；固定一份当前预览，10 分钟后禁止导出，后续刷新替换预览。该期限是导出有效期，不是后台自动销毁定时器。
- 原生保存对话框与写入在 spawn_blocking 中执行，导出期间以独立 gate 拒绝重复导出、清空和替换预览；不在持有记录锁时等待用户。导出字节在弹窗前从已验证预览取得，后台新事件不改变该报告。
- 只创建用户所选的新文件，create_new 拒绝覆盖已有文件，Windows 写入期间禁止共享访问；拒绝相对路径、备用数据流和设备命名空间及常见保留设备名。不将这一导出入口声称为对恶意祖先目录替换的完整文件系统沙箱。
- 取消不写文件；写入及同步失败可能留下不完整文件，错误明确提示核对并另选文件名，不自动重试、不尝试按路径删除不确定目标。报告不打包已有应用日志、配置、数据库、恢复副本或原生会话。
- 控制器串行化刷新/清空/导出，忽略卸载后迟到结果，通信错误不重放导出；清空请求发出前撤销旧预览。界面仅显示后端固定白名单错误，未知 IPC 文本使用通用提示，避免重新引入任意日志泄露。
- 诊断操作纳入设置离开与窗口关闭保护；导出或确认等待期间不卸载面板、不销毁窗口，取消和失败释放 busy。补充控制器与真实关闭处理器的集成回归，保留既有窗口关闭权限、退出草稿锁及防重入修复。
- 9 项后端诊断回归覆盖容量/合并、快照一致性与失效、清空互斥、新文件导出不覆盖、设备路径拒绝、真实缺失程序、包含凭据样式 stderr 的真实子进程、命令失败/超时/退出，以及真实非法 JSON 和截断帧。旧 stderr 原文尾部测试随实现移除，由新的隐私和有界记录测试替代，不静默跳过。
- 前端新增 8 项回归，覆盖导出准确快照、原生取消、重复操作、清空后刷新失败、确认取消、卸载迟到结果、未知结果不重放、错误白名单及关闭保护。
- 最终 Rust 210 项通过、0 失败、0 跳过，显式包含联网模型目录、隔离运行时安装/验证/回滚和真实 Pi 会话；前端 179 项通过。Svelte/TypeScript 零错误零警告、Clippy -D warnings、许可证检查通过，网页和桌面构建成功。
- 已更新 src-tauri/target/debug/deeppi.exe；原有 SSR Channel 未使用和 Windows 链接器提示仍存在。更新 TROUBLESHOOTING.md 的入口、事件码、隐私与保留策略说明；本轮未新增依赖。
- 浏览器安全拒绝仍有效，未重试、换端口或接管桌面绕过。原生保存/取消、设置焦点、关闭往返、窄窗口和主题尚未验收。当前诊断只覆盖 Pi RPC 传输与进程，不代替 DSH/TUI/安装诊断、崩溃报告或跨重启日志。
- 未暂存、提交或推送当前开发仓库。U2/U3/U4/U5/U6 其他余项、真实认证、特殊 Git 流程与持久恢复、崩溃草稿及长稳验收仍保留，整个重设计目标继续 active。

## 27. U3 项目文件监听与刷新合并

2026-09-09（本地时间），补齐持续文件变化通知主链，U3 界面和性能验收仍未完成：

- 引入 notify 8.2.0，使用其 Windows ReadDirectoryChanges 实现，不自行解析原生通知缓冲区。已亲读锁定版本的注册、句柄共享、回调及 Drop 源码；Cargo 增加 7 个依赖条目，许可证检查通过。
- start_project_watch/stop_project_watch/ping_project_watch 均 main-only，监听根只按项目 ID 从 TaskStore 获取。启动前复用 GuardedPath 的祖先和 reparse point 检查，保留保护句柄直到原生监听注册返回；之后释放，不以长期独占根目录阻止正常目录移动。
- 只监听当前选中项目，前端切项目或销毁后按确切 projectId/watchId 释放。后端最多注册 8 个临时租约，容纳切换期间的在途启动；新启动清理已结束 worker 的记录，旧关闭请求不能结束新租约或其他项目监听。
- 前端 30 秒续期一次，同一租约不并发续期；5 分钟无续期时 worker 发出过期状态并停止，覆盖页面丢失、清理 IPC 失败等情况。页面挂起恢复后若租约失效，会显示重新连接入口；没有声称睡眠/后台节流下永不断开。
- 后端回调仅累积原子刷新位，不保留事件路径列表或文件内容，每秒最多合并发送一次文件/Git 刷新标记。返回 projectId/watchId 和固定状态，前端拒绝旧租约和其他项目事件。
- 普通项目路径触发文件和 Git 刷新；.git 及 node_modules/target/build 等已排除目录的后代只触发 Git，避免构建产物持续重做文件索引和全文搜索。与排除目录同名的普通文件仍会触发文件刷新，目录自身变化最多带来一次额外索引核对。
- 每 5 秒按安全句柄重新核对根身份；根被替换、不可访问或监听错误时停止该租约并提示重新连接，不静默将原监听迁移到新目录。只有刷新标记跨 IPC；实际读取仍经过现有项目路径/文件版本验证。
- 锁定版本 notify 的 Windows 实现存在通知不完整或静默停止的可能，不能仅凭对象存活证明通知完整。因此每 30 秒额外发出一次文件/Git 核对标记；复用现有有界索引/读取，而不是后台无限扫描或对每次输入重新遍历。
- 上述周期是线程调度预算，不是文件系统调用或完整刷新链的硬实时期限。启动、停止、续期管理进入 spawn_blocking；显式停止唤醒并等待自有 worker，库的原生事件线程按其 Drop 机制异步退出。慢速/断开的网络文件系统可能增加收尾等待，未完成此场景的原生验收。
- 文件监听接到页面级刷新状态，文件区与 Git 区分开失效；隐藏的文件/Git 视图不因通知立即执行完整读取，重新显示时核对。文件树保留原展开/搜索状态和按路径标识的行，未新增 Ctrl+P 浮层。
- 内容查询和 Git 状态读取改为“当前操作结束后补一次”，连续外部通知不再不断取消同一查询。用户更换查询/项目、显式取消和卸载仍沿用原请求代次与取消机制；新刷新不会绕过 Git 信任确认。
- 已先复现读取/保存期间的外部刷新直接丢失，再增加每文档单个 pending 标记。忙状态结束后补读，另存时转移到新文件身份；关闭和移除项目清理标记，旧文档迟到结果不触发新实例多余读取。
- 退出或项目移除锁期间暂存刷新，取消/失败解锁后补读。读取结果继续保护未保存草稿及输入期间的新内容，仅更新磁盘快照和冲突提示，不通过自动监听调用强制重载或保存。
- 后端新增 8 项回归，包含真实目录批量写入、根替换、租约过期、跨项目/旧 ID 拒绝、路径分类，以及实际等待 30 秒的无原生事件兜底刷新；另验证根身份保护句柄不会阻止同一父目录下的运行时回滚。
- 前端新增 9 项回归：4 项监听代次/迟到初始化/根失效/续期清理，5 项读写期间合并补读、另存转移、退出解锁和关闭后重开隔离。完整前端 188 项通过，Svelte/TypeScript 零错误零警告。
- 首次全量 Rust 运行 217 项中，既有 recovery::tests::failed_verification_restores_previous_directory 有 1 项失败，原断言没有输出底层错误。保留了失败临时夹具，确认 current/candidate 仍存在；增强断言以显示实际错误，没有放宽断言或改变生产回滚代码。13 项相关测试复跑通过，新增目录锁干扰回归也通过，但首次失败原因尚未证实，不能据复跑称其根因已修复。
- 最终完整 Rust 218 项通过、0 失败、0 跳过，继续显式执行联网模型目录、隔离运行时安装/验证/回滚和真实 Pi 会话测试；Clippy -D warnings、许可证检查、网页与桌面构建通过。
- 已更新 src-tauri/target/debug/deeppi.exe。SSR Channel 未使用提示现同时涉及 ChatPane 和页面组件，Windows 链接器提示仍存在；未通过提高阈值或禁用检查掩盖警告。
- 不监听授权项目根以外的额外目录。嵌套项目或 linked worktree 的外置 Git 元数据没有单独订阅，现由定期核对及原有焦点/手动刷新覆盖，不声称所有 Git 元数据都能即时通知。
- 未访问被拒的浏览器页面、未接管桌面，未启动额外服务绕过限制。真实文件树焦点/滚动、编辑器往返、连续 Git 操作、大仓库开销、网络文件系统和 8 小时长稳仍待验收；没有暂存、提交或推送。整个重设计目标继续 active。

## 28. U6 共享快捷键定义与作用域

2026-09-09（已核对本机日期），快捷键一致性实现完成，追加检查与原生验收仍待完成：

- 新增 shortcuts.ts，将 7 个宿主命令与 2 个文件差异命令的按键、修饰键及作用域集中定义。菜单标注、工具提示、aria-keyshortcuts 和 CodeMirror 绑定由该定义生成；navigation.ts 的兼容入口也复用同一匹配器。
- 菜单和键盘共用 hostCommandEnabled 与 runHostCommand，不再分别维护保存、另存、侧栏和对话输入的执行条件。文件就绪判断使用文件控制器的规范化 ID，避免路径大小写导致菜单与实际编辑文档不一致。
- 宿主区分“不处理”“消费但不执行”和“执行”。长按重复事件不重复调用命令；活动文件加载或保存期间消费保存组合键，防止穿透到 Webview 的页面保存。没有活动文件时保留终端 Ctrl+S，对话输入命令仅适用于 Pi RPC 工作区，不接管 TUI 的 Ctrl+L。
- 对话框、菜单和恢复面板持有操作权时，宿主快捷键不抢占；退出准备期间继续冻结键盘交互。受保护的设置操作期间禁止任务/文件导航，菜单和键盘保持相同状态。
- 识别标准 IME composition 及 keyCode=229 兼容信号，拒绝 Alt/AltGraph 和同时按 Ctrl+Meta 的额外组合。沿用 Meta 别名，但不将操作系统预先截获的按键声称为可由宿主控制。
- 保存按钮、搜索框、对话输入和差异导航补充共享的可访问性标注。文件比较继续仅在自身只读编辑区处理 F7/Shift+F7；编辑器原有文本移动和撤销配置保留，不虚构未接入的编辑能力。
- 新增 KEYBOARD_SHORTCUTS.md，描述实际操作、范围及优先级；文档同步测试检查每个注册组合键均有对应条目。未新增快速打开浮层或占据工作区的快捷键介绍页面。
- 独立核验曾质疑修饰键判断。主代理复核代码后确认该条件要求 Ctrl/Meta 恰有一个为真，并补充四种组合、裸字母和仅 Shift 输入的回归；测试排除了“裸字母触发宿主命令”的判断，没有依据错误结论修改正确逻辑。
- 新增 13 项测试：12 项共享定义、文档同步、作用域、重复按键、输入法、裸键和模态优先级，1 项在真实 CodeMirror 状态中检查两侧差异绑定及 composition 拒绝。最终完整前端 201 项通过。
- 业务代码完成后的 Svelte/TypeScript 检查为零错误零警告，网页与桌面构建成功，更新 src-tauri/target/debug/deeppi.exe；之后只追加了一项裸键/修饰键测试，没有修改业务代码。
- 追加测试后的最后一次 pnpm check 被审批服务拒绝，返回原因为审批服务订阅额度不足。该命令未执行，未换命令、工具或执行方式绕过；等待用户明确确认再尝试。不得将此前检查结果冒充这一次检查成功。
- 本轮未修改 Rust 或依赖，未重跑后端/联网测试；第 27 节的 218 项含联网通过仍是最近后端结果。那次未复现的目录回滚测试失败原因仍未确认。SSR Channel 未使用及 Windows 链接器提示仍存在。
- 没有访问被拒的浏览器或接管桌面。DSH 子 Webview 内的宿主快捷键转发、真实焦点/IME/屏幕阅读器、主题布局和长期运行仍未验收；未暂存、提交或推送，整体目标不标完成。

## 29. 启动异步化、操作门控与 managed-only 边界收口

2026-09-10（本地时间），补齐此前未写入文档的一批实现并完成基线收口；本节改动随后进入基线提交：

- 应用初始化移出窗口事件循环（当日已实现但未记录，本轮以测试补验）：Rust setup 只启动后台 worker，新增 `StartupState` 与 `await_startup`，未就绪的业务 IPC 统一拒绝；前端 `loadAppStartup` 并行读取设置、任务与项目，运行时探测作为后台任务，不阻塞工作区首次可用。初始化失败时前端显示错误并保持不可用，不做自动重试；原生启动时序仍未人工验收。
- Provider/Runtime/Package 写入串行化（当日已实现但未记录，本轮以测试补验）：新增 `ProviderConfigGate`，包操作复用 `PackageOperationLock`，在存在 TUI 或 RPC 任务时拒绝修改；PTY 与 RPC 启动前统一读取设置与并发容量。
- 原生 Pi 环境（managed/native）实验后按“v1.0 仅支持托管运行时”收口：旧 native 任务记录、旧设置与数据库列保留用于兼容，但不再提供启动或恢复路径。
  - 删除死代码 `app_paths::native_pi_home`；`task_pi_home` 只解析托管绑定，旧 native 任务返回明确提示；PTY/RPC 删除不可达的 native 分支并统一移除 `PI_CODING_AGENT_SESSION_DIR`；扩展市场去掉“本机 Pi（仅查看）”死 UI；重启旧 native 任务时直接给出明确提示，不进入原生配置。
  - 设置中的 `auto`/`native` 仍迁移为 `managed`（真实读写测试覆盖），数据库迁移与检查约束不变；`import_native_sessions` 保持仅测试可见。
- 新增/修正回归：旧 native 任务数据保留但不可启动、不可换用其他会话文件；托管任务解析其绑定的 `pi_home`。
- Rust 并行测试修复：`GitRepository::open` 的测试专用预算由 30 秒调整为 300 秒（生产请求仍为 30 秒），消除默认并行下 `git_*` 因进程/磁盘争用出现的“总等待预算”假失败；预算与取消语义仍由 `git_operation` 与 `git_index` 的专项测试覆盖。
- `cargo fmt` 全量格式化（此前 CI 的 `fmt --check` 未通过）；修复 `task.rs` 中 `HashMap` 仅测试使用导致的 clippy unused-import。
- 验证：`cargo test` 默认并行 230 通过 / 0 失败 / 3 ignored；`cargo test -- --include-ignored` 233 通过 / 0 失败（含联网模型目录、隔离运行时下载与真实 Pi 会话）；`clippy -D warnings` 与 `fmt --check` 通过；`pnpm check` 0 错误 0 警告；前端 210 项通过。
- 本轮未访问被拒绝的浏览器页面、未接管桌面；原生交互、主题/DPI 与 8 小时长稳仍未验收。U1b–U6 的界面与原生验收在后续章节继续推进。

## 30. 基线提交与 U1b/U2 自动化审计

2026-09-10（本地时间），目标 v1.0 确认后的第一批收口：

- 基线提交：`4e9975e` 后端切片、`83adefb` 前端、`567eaf8` 文档与仓库卫生。工作区干净，`main` 与 `origin/main` 一致。GitHub HTTPS 直连被阻断，改用已认证 SSH 推送成功，本地 remote 已改为 `git@github.com:springkai66/deep-pi.git`；推送触发 CI run `34498817897`（截至本节写入仍在进行）。
- U1b 自动化审计：`cargo test --lib project_files::` 14 项通过；前端 `files` / `file-workspace` / `file-comparison` / `project-watch` 共 38 项通过。六条验收标准逐项有代码与测试证据：Ctrl+P 聚焦文件搜索不打开浮层；“任务/文件”切换与常驻搜索；根目录按项目 ID 从 TaskStore 解析，`validate_relative` 拒绝绝对路径/穿越/保留设备名；有界遍历与 `.git`、`node_modules`、构建目录排除；模糊过滤保留父目录且不修改展开集合；预览 2 MiB 上限、NUL/非 UTF-8 拒绝、读取前后元数据与身份校验、过期结果忽略；junction/reparse point 与根替换拒绝。
- U2 自动化审计：命令白名单、事件映射、扩展 UI 交互、停止（`clear_queue` + `abort`）、Ctrl+L 聚焦对话输入、taskId+runId 订阅与过期过滤均有实现与测试；新增 [RPC_COMPATIBILITY.md](./RPC_COMPATIBILITY.md)，按托管 Pi 0.84.4 的官方 `docs/rpc.md` 锁定已验证的命令、事件、扩展 UI 方法与原生会话恢复方式，并列出接收但不映射的事件与未知扩展交互的兼容模式行为。
- 原生交互验收（U1b–U6 共 6 个任务）仍待执行：当前环境没有可用的桌面自动化工具，浏览器访问仍被拒绝。计划在可自动化审计全部完成后产出一份 `NATIVE_ACCEPTANCE.md` 清单，由用户在本机逐项操作并回报结果。
- 未修改业务代码，未新增依赖；`cargo fmt`/`clippy` 状态与第 29 节相同。

## 31. CI 首次运行失败修复

2026-09-10（本地时间）：

- 推送 `567eaf8` 后 CI run `34498817897` 在 “Run Rust tests” 失败：3 项 content search 测试在 GitHub runner 上因没有 `rg.exe` 直接 unwrap 失败；`real_pi_pages_a_large_native_session_without_replaying_prompts` 因 runner 没有 Pi SDK 断言失败。
- 修复：content search 测试在缺少 ripgrep 时验证“未找到 ripgrep”降级路径并跳过后续断言（CI 通过 `taiki-e/install-action` 安装 ripgrep，继续覆盖真实搜索）；SDK 依赖测试标记 `#[ignore]`，与同文件另一个真实 Pi 测试一致，由 `--include-ignored` 在装有 SDK 的环境执行。
- 本地复验：`cargo test` 229 通过 / 0 失败 / 4 ignored；`cargo fmt --check` 与 `clippy -D warnings` 通过。远端 CI 结果待观察。
- 第二次运行 `9e4ae05`：Rust 测试已通过（ripgrep 安装与 ignore 生效），但 `gitleaks/gitleaks-action@v2` 在下载阶段失败：上游 8.24.3 的 Windows 资产是 `.zip`，action 仍请求 `.tar.gz`，得到 HTTP 404。改为在 workflow 内固定下载 `gitleaks_8.24.3_windows_x64.zip` 并运行 `gitleaks git`，checkout 改为完整历史以便扫描仓库历史。
- CI ripgrep 安装核实：`taiki-e/install-action` 并不支持 ripgrep（会提示 fallback），实际回退 `cargo-binstall`，从 GitHub 下载 ripgrep 15.2.0 并加入 PATH；CI 中的内容搜索测试确实运行了真实 `rg`。

## 32. U3/U4 自动化审计

2026-09-10（本地时间）：

- U3：全文搜索为文件侧栏内的独立“内容”模式，与文件名过滤分开；结果按文件分组并标注行列，支持取消、截断提示与不可读文件计数。ripgrep 先探测后使用，缺失时返回明确错误且不自动安装；测试在缺少 rg 的环境验证该降级路径，CI 安装 ripgrep 继续覆盖真实搜索。新增 400 文件/20 目录的规模回归（40 命中、15 秒预算内）。外部编辑器仅接受 Code.exe / Code - Insiders.exe / notepad++.exe 的绝对路径，参数数组启动、不经 shell，传递文件与行列；空格/中文/shell 字符路径、错误编辑器类型、未配置时引导设置均有测试覆盖。文件变更刷新沿用例 27 的合并与补读逻辑。
- U4：状态、分支、远程与未跟踪/已暂存/未暂存分类由 `git_status` 测试覆盖；差异覆盖文本、二进制、重命名、删除、冲突与无 HEAD；显式暂存/取消暂存由索引事务测试覆盖，提交与推送分离，准备提交显示选中范围，推送显示目标分支且拒绝非快进；参数数组、有界输出、超时/取消、系统凭据复用与“不回显凭据”均有测试；并发索引变更与锁冲突由 `existing_lock_is_never_removed_or_overwritten`、`blocks_other_git_writers_while_transaction_is_active`、`publishes_only_on_commit_and_preserves_unrelated_staging` 覆盖；界面不提供“Agent 修改”归属，不会把用户改动标为 Agent 产物。
- 两项待人工验收：获准的真实远程提交/推送（不以本地 bare remote 冒充）、外部编辑器与本机原生交互；已纳入待编写的 `NATIVE_ACCEPTANCE.md`。

## 33. U5/U6 自动化审计与原生验收清单

2026-09-10（本地时间）：

- U5：`project_edit` 11 项测试覆盖版本冲突、并发替换、create-only 另存、只读文件、junction 父目录、二进制/超限与删除后不重建；`file-workspace` 25 项覆盖草稿与撤销隔离、退出锁、BOM 与混合换行保留、关闭确认、保存期间继续输入、刷新不覆盖草稿与未知结果保留。编辑器比较与消息渲染按需加载（`FileComparison`、`ChatMessage`、`message-markdown` 动态 import），未进入首屏包。中文输入法、真实文件往返与屏幕阅读器仍待原生验收。
- U6：六类设置分类与键盘导航有实现与测试；快捷键展示与实现共享定义并有文档同步测试；DSH 菜单遮挡切换已实现（原生覆盖关系待验收）。DSH 子 Webview 内宿主快捷键转发仍未实现，`KEYBOARD_SHORTCUTS.md` 已如实标注；实现需受限原生路由（不向子 Webview 开放 IPC），成本与风险较高，列为待用户决定项。
- 新增 [NATIVE_ACCEPTANCE.md](./NATIVE_ACCEPTANCE.md)：按 U1b–U6、运行时升级与 DSH 分组给出逐项操作与预期，并明确回报方式；完成原生验收前不将 U1b–U6 视为最终验收完成。
- 包体积核验（1.0.0 构建）：`build/index.html` 首屏 modulepreload 共 11 个文件，gzip 合计约 34 KB；含 CodeMirror 的编辑器/比较块（最大 339 KB raw / 87 KB gzip）与终端块均为按需 chunk，不进入首屏；客户端产物总量约 2.8 MB，分布在按需块中。

## 34. 发布工程收口与本地构建

2026-09-10/11（本地时间）：

- 版本统一为 1.0.0：`package.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml` 与 `Cargo.lock` 一致。
- 按“未签名发布 + 签名并行申请”策略改造 `release.yml`：Authenticode 改为检测 `DEEPPI_WINDOWS_CERTIFICATE_BASE64` 后自动启用，未配置时打印未签名提示并继续；updater minisign 三个 secrets 仍为 tagged 发布必需。新增 [RELEASING.md](./RELEASING.md)（secrets 清单、密钥生成命令、发布与回滚步骤）与 [RELEASE_NOTES.md](./RELEASE_NOTES.md)；README 增加下载安装、SmartScreen 说明、已知限制与文档索引。
- 本地构建：release 构建成功，产出 `DeepPi_1.0.0_x64-setup.exe`；`installer-smoke` 完成安装与卸载 PASS；`release-check --require-installer` 通过；`updater:prepare` 使用占位公钥 dry-run 通过（生成文件已删除）。
- MSI 未能在本机构建：Tauri 需从 GitHub 下载 WiX 3.14，而本机无法直连 GitHub；choco 安装因 `C:\ProgramData\chocolatey\lib` 锁文件无权限修复而失败。MSI 由 GitHub runner 在 Release workflow 构建（历史 run 已验证 NSIS+MSI 构建）。
- 后续已本地补齐 MSI：从 nuget.org 取 `wix.3.14.1.nupkg`，将 `tools/*` 解压到 `%LOCALAPPDATA%\tauri\WixTools314`，`pnpm tauri build --bundles msi` 成功产出 `DeepPi_1.0.0_x64_en-US.msi`；`release-check --require-installer` 现在同时识别 NSIS 与 MSI。该变通写入 TROUBLESHOOTING.md。
- 安装包内容核验：`deeppi.exe` 与 NSIS 安装包 FileVersion 均为 1.0.0；MSI 用 `msiexec /a` 管理安装解包成功（不修改系统），载荷 `deeppi.exe` 版本 1.0.0，解包目录已清理。
- CI：修复后 `808e944` 与 `1b673fb` 均为 success（含 cargo audit、licenses、release check、web build）。后续 tag 发布需用户先配置 updater secrets。
- 待用户：配置 GitHub Secrets；按 `NATIVE_ACCEPTANCE.md` 完成原生验收；决定 DSH 子 Webview 宿主快捷键转发是否纳入 v1.0。

## 35. 本机全量测试稳定性修复

2026-09-11（本地时间）：

- Git 命令级 10 秒上限移除：单个 git 命令的时长改由整体等待预算约束。本机安全软件拦截进程创建时，正常 git 调用会超过 10 秒被误判为超时，表现为“无法定位项目的 Git 工作树和元数据目录”；生产读预算仍为 30 秒、同步仍为 180 秒。
- 推送取消测试的服务器钩子等待窗口由 15 秒放宽到 30 秒，取消后快速返回断言放宽到 20 秒。
- 内容搜索规模测试拆分：默认套件保留 4 目录/40 文件的跨目录回归；1200 文件规模检查标记 `#[ignore]` 单独运行。测试构建的 `MAX_SEARCH_TIME` 放宽为 90 秒（生产仍 10 秒），避免新写入大批临时文件时被安全软件实时扫描造成假超时。
- 证据：默认并行 `cargo test` 连续两次 230 通过 / 0 失败 / 5 ignored；`cargo test -- --include-ignored --test-threads=6` 235 通过 / 0 失败 / 0 ignored。
- 环境限制：本机 SkyGuard 向 `git.exe` 注入钩子，重负载下偶发退出码 `0xC0000005`（git.exe 自身崩溃）；CI 干净 runner 无此问题。该现象已在实施记录中保留，不用它作为放宽生产断言的依据。

## 36. Release workflow 未签名预演（workflow_dispatch）

2026-09-11（本地时间）：

- 通过 GitHub API 对 `main` 触发 `Windows Release` 的 workflow_dispatch（非 tag 路径），预演完整发布流水线：release checks → `pnpm test`（check + web + cargo）→ 构建 NSIS+MSI → installer-smoke 安装/修复/卸载 → 上传产物。
- 结果：run `34565555612`（head `d3d36bd`）**success**；产物 `deeppi-windows-d3d36bd...` 大小 13,804,138 字节（bundle 目录，含 NSIS 与 MSI），未过期；tagged-only 步骤（updater secrets、签名、发布与 stable 通道）按预期跳过。
- 结论：发布流水线在干净 runner 上可用，MSI 所需的 WiX 下载在 GitHub runner 正常。只差用户配置 updater secrets 后打 `v1.0.0` tag。
- 同期 CI：`c3a92cc`、`cb22ae2`、`e997487`、`d3d36bd` 全部 success，main 为绿。

## 37. Updater 产物链本地预演（一次性测试密钥）

2026-09-11（本地时间）：

- 用本地一次性 minisign 测试密钥预演 tagged 发布产物链：`updater:prepare` 生成含测试公钥的 release config → `tauri build --bundles nsis --config src-tauri/tauri.release.generated.conf.json` 产出 `DeepPi_1.0.0_x64-setup.exe` 与 `.exe.sig` → `updater:manifest` 生成 `latest.json`（version 1.0.0、HTTPS 更新 URL、signature）→ `node scripts/release-check.mjs --require-installer --require-updater` **PASS**。
- 关键发现：本机 Tauri CLI 只认 `TAURI_SIGNING_PRIVATE_KEY`（密钥内容），未识别 `TAURI_SIGNING_PRIVATE_KEY_PATH`；预演与 `release.yml` 均使用内容方式注入，一致。
- 测试密钥文件与生成的 release config 已删除，仓库与 Secrets 未受影响。tagged 发布只差用户配置三个 updater secrets。
- Release 说明改用仓库内 `RELEASE_NOTES.md`（`body_path`），不再自动生成提交列表，保证 Release 页面包含安装步骤、已知限制与反馈方式。

## 38. DSH 子 Webview 宿主快捷键：原生加速键路线实测不成立

2026-09-11（本地时间）：

- 曾实现「隐藏原生菜单 + 加速键 + Tauri `on_menu_event` 转发」（提交 `c69f438`）：DSH 可见时为主机作用域命令 `Ctrl+Shift+P` / `Ctrl+P` / `Ctrl+,` 注册加速键，经白名单转发 `host-shortcut` 事件给主 Webview，不向 DSH 页面开放 IPC；Rust/前端单测、CI 与发布预演均通过。
- 自动化原生按键验收发现该路线在本架构下不成立（Win32 `keybd_event` 注入 + `PrintWindow` 截图 + 应用日志核对）：
  - DSH 子 Webview 聚焦时按 `Ctrl+P`，出现的是 WebView2 自带的打印对话框，宿主菜单事件未触发；
  - `Ctrl+,`（非 WebView2 内置快捷键）同样没有触发菜单事件；日志只有 `event=dsh_shortcuts status=enabled`，没有任何 `dsh_shortcut_menu status=received`。
  - 结论：焦点在 WebView2 子窗口时，按键由 WebView2 自行处理或不会进入宿主的 `GetMessage` / `msg_hook` 链路，`TranslateAcceleratorW` 无法命中；Tauri 菜单加速键只适用于焦点在宿主窗口自身的情况。
- 处理：撤销该实现（删除 `dsh_shortcuts` 模块、`lib.rs` 菜单与事件转发、前端 `host-shortcut` 监听与 `set_dsh_shortcuts` 调用及相关单测），文档恢复为「DSH 子 Webview 聚焦时宿主快捷键不生效」的已知限制。
- 正确方案（待实现，v1.0.x）：使用 WebView2 的 `AcceleratorKeyPressed` 事件（`ICoreWebView2Controller`，经 `Webview::with_webview` 获取），在 DSH 子 Webview 创建时注册处理器，命中白名单组合键时置 `Handled` 并向主 Webview 发送同一套 host 命令；需要把 `webview2-com` 提为直接依赖并处理 COM 回调生命周期。
- 保留可复用的自动化验收方法：空闲检测（`GetLastInputInfo`）→ 启动 debug 应用 → `PrintWindow` 截图 → 点击左侧导航 → `keybd_event` 注入组合键 → 再次截图 → 查日志；证据在 `artifacts/ui-check*`（gitignored）。

## 39. 最终预发布验证（工程侧就绪）

2026-09-11（本地时间）：

- 代码与测试（当前 `main` = `28899ef`）：默认并行 `cargo test` 连续两轮 **232 通过 / 0 失败 / 5 ignored**；`cargo test -- --include-ignored --test-threads=6` **237 通过 / 0 失败 / 0 ignored**（含联网模型目录、隔离运行时下载与回滚、真实 Pi 会话）；前端 34 个文件 **213 项通过**；`pnpm check` 0 错误 0 警告；`cargo fmt --check` 与 `cargo clippy --all-targets -- -D warnings` 干净。
- 发布流水线：在当前 HEAD（含 DSH 快捷键与测试稳定性修复）上再次 `workflow_dispatch` 预演 `Windows Release`，run `34592658950` **success**：release checks → `pnpm test` → NSIS+MSI 构建 → installer-smoke 安装/修复/卸载 → 产物上传；tagged-only 步骤按预期跳过。
- CI：`c69f438`、`28899ef` 均为 success，`main` 全绿。
- 追加无界面运行时冒烟（托管运行时，临时 DSH_HOME）：按应用相同参数启动 `node <dsh cli> web --host 127.0.0.1 --port 0 --no-open`，解析到与应用一致的 `dsh web: http://127.0.0.1:<port>`，HTTP 200，结束后进程树无残留托管 `node`。Pi 侧的真实发送/工具/停止/恢复/历史分页已由 `--include-ignored` 集成测试覆盖；DSH 原生窗口嵌入与 Pi TUI 交互仍需按 NATIVE_ACCEPTANCE.md 人工验收。
- 原生启动与界面验证（自动化）：用当前代码重建 debug 应用并启动，主窗口出现且标题为 `DeepPi`，日志 `event=app_initialization status=ready duration_ms=20`，无 `dsh_shortcuts install_failed` 警告，结束无残留进程；`PrintWindow` 截图（`artifacts/v1.0.0-main-window.png`）确认顶部仍为自绘 HTML 菜单栏、无原生菜单条、侧栏与工作区布局正常——即隐藏快捷键菜单未引入可见菜单栏或布局回归。DSH 聚焦时的实际按键路由仍需人工实按验证。
- 升级回滚实测：`runtime::tests::installs_verifies_and_rolls_back_live_pi_in_isolation`（`--include-ignored`）在当前提交通过（200 秒），在隔离 profile 中完成官方 Pi 运行时的下载、安装、验证与回滚。结合 installer-smoke（安装/修复/卸载）、启动冒烟与无界面 DSH 冒烟，task-9 可自动化的条款已全部取证；应用内 GUI 交互（建 Pi 任务、嵌入 DSH、应用内升级）按 NATIVE_ACCEPTANCE.md 由用户执行。
- 说明：最终本地产物（`DeepPi_1.0.0_x64-setup.exe` / `DeepPi_1.0.0_x64_en-US.msi`）已用第 38 节回退后的 `main` 重新构建，并复跑 `installer-smoke`（安装/修复/卸载 PASS）与 `release-check --require-installer`（PASS）。
- 结论：工程侧 v1.0 就绪。剩余仅两项依赖用户：配置 updater minisign secrets 后由我打 `v1.0.0` tag 发布；按 `NATIVE_ACCEPTANCE.md` 完成原生验收（含 DSH 快捷键三项检查）并回报结果。

## 40. 自动化原生验收：主窗口、设置、快捷键、文件索引、DSH 嵌入与任务创建

2026-09-11（本地时间，debug 构建 = `main@2c0fd74`）：

- 方法：空闲检测（`GetLastInputInfo`）→ 启动应用 → `PrintWindow` 截图 → 鼠标点击导航/菜单 → `keybd_event` 注入快捷键 → 再次截图 → 对比截图与应用日志；证据在 `artifacts/accept-a/`（gitignored）。
- 结果：
  - 顶部“文件 / 设置 / 帮助”均以自绘 HTML 菜单展开，无原生菜单条（截图 02-04）。
  - 设置页六类齐全（通用/外观/模型与凭据/运行时与更新/Pi 扩展/高级与诊断）；外观页的颜色模式与字体项正常渲染（05、06）。
  - `Ctrl+Shift+P` 聚焦并选中任务搜索（07）；`Ctrl+P` 切到“文件”页签并聚焦文件搜索（08）；`Ctrl+,` 打开设置（09）。
  - 文件页签对一个真实大项目（Hotta，索引 13382 个文件）正常渲染并显示计数，加载中状态可见（10）。
  - DSH 子 Webview 在窗口内成功渲染 DSH 起始页（侧栏/新建会话/输入区，见上一轮 `artifacts/ui-check/02-dsh-view.png`）。
  - 点“+ 新建任务”创建 RPC 任务成功：出现任务页签、对话输入区与“等待输入”状态；模型显示 `unknown`，因为托管 Pi 尚未配置 provider（本机预期状态，11、12）。
  - 全程结束后无残留 `deeppi` / 托管 `node` 进程。
- 清理：自动化创建的任务 `Pi Task 3` 已从应用数据库删除，其空的会话目录也已移除，未留下测试数据。
- 仍需用户：真实模型回复（需先在托管 Pi 配置 provider）、应用内运行时升级 UI、主题/DPI/IME、DSH 聚焦快捷键（已知限制）。

## 41. 自动化原生验收：设置各页、文件搜索与预览（含一次误报澄清）

2026-09-11（本地时间，debug 构建）：

- 方法：写空闲窗口等待器（连续 36 秒无输入才开始），再用鼠标点击 + 剪贴板粘贴（避开中文 IME 把按键转成拼音串）+ `PrintWindow` 截图。
- 通过：
  - 设置 → 运行时与更新：DeepPi 1.0.0 / Pi Coding Agent 0.84.4 / DeepSeek Harness 0.1.1-rc.2 / DSH Plugin Market 1.40.0，均标注“托管”。
  - 设置 → 模型与凭据：Provider 表单（预置/标识/API 类型/Base URL/代理/Header）与官方模型目录搜索、Credential Manager（API Key + 保存 Key + 测试连接）。
  - 设置 → 高级与诊断：外部编辑器、Pi RPC 诊断（隐私声明“不含 stderr 原文、提示词、文件内容、路径与凭据”、批次过滤、报告 JSON、导出）、运行环境列表。
  - 文件搜索：`package` / `js` 均正确定位并展开命中路径（保留父目录）。
  - 文件只读预览：点击 `.../protobuf/src/google/protobuf/compiler/java/java_doc_comment_unittest.cc` 后右侧打开完整预览：行号、C++ 语法高亮、完整路径、底部 `UTF-8 · CRLF · 已保存`，工具栏含外部编辑器/差异/查找/刷新/关闭。
  - 大项目索引：Hotta 项目 15000+ 文件，底部计数随索引推进更新。
- 澄清一次误报：先前“无法打开预览”是自动化脚本点击坐标落在目录行、且中文输入法把 `TypeText` 转成拼音所致；修正为剪贴板输入 + 文件行坐标后确认预览正常，**不是应用缺陷**。
- 仍未验证：真实模型回复、应用内运行时升级 UI、关闭确认弹窗（当前设置为“退出应用”）、主题/DPI/IME、DSH 聚焦快捷键。

## 42. 自动化原生验收：主题、窄窗口、侧栏项目过滤与一次误操作

2026-09-11（本地时间，debug 构建）：

- 通过：
  - 浅色主题：将 `colorMode` 置为 `light` 后启动，界面变为浅色（浅色背景、深色文字、绿色主按钮），说明主题体系按设置生效；随之恢复为 `dark`。
  - 窄窗口：窗口缩到 820×700 后布局正常，侧栏/工具栏/工作区无横向溢出或遮挡。
- 澄清：任务侧栏只显示 Hotta 是**正确行为**，不是缺陷：数据库中 `deep-pi` 与 `project-b` 的 `removed_at` 非空（已在之前会话中从工作区移除），`list_projects` 按 `removed_at IS NULL` 过滤。
- 一次误操作与修复：自动化脚本用 PowerShell `ConvertTo-Json` + `Set-Content -Encoding UTF8` 写入 `settings.json` 时引入了 BOM，应用正确报出 `settings.json is invalid: expected value at line 1 column 1` 错误页。已用 UTF-8 无 BOM 重写并校验（内容与原值一致、`colorMode=dark`），应用随后启动正常；同时确认 `settings-*.json` 备份机制正常。这也是一个正向证据：无效配置不会静默丢失，而是显示可重试的错误页。
- 仍未验证：真实模型回复、应用内运行时升级 UI、关闭确认弹窗（当前 `closeBehavior=exit`）、高 DPI/多显示器、IME、DSH 聚焦快捷键。

## 43. 自动化原生验收：关闭确认、DSH 崩溃隔离与恢复入口

2026-09-11（本地时间，debug 构建；临时修改的设置已恢复）：

- 关闭确认：临时将 `closeBehavior` 改为 `ask` 后启动，点窗口关闭按钮弹出居中模态“关闭 DeepPi”（最小化 / 退出应用两个选项，说明“这个选择会记住，也可以在设置中修改”）；按 Escape 取消后应用继续运行，弹窗关闭。随后已恢复 `closeBehavior=exit`（当前设置下直接退出是符合配置的行为）。
- DSH 崩溃隔离：打开 DSH 页面确认渲染正常，找到应用的直接子进程（`...\com.deeppi.desktop\runtimes\node\current\node.exe ...@deepseek-ai\dsh\lib\bin.js web --host 127.0.0.1 --port 0 --no-open`）并 `taskkill /F` 杀掉。应用**不退出**，界面切到错误态：`DSH exited with code 1` + “重启 DSH”按钮，符合“DSH 崩溃不导致 DeepPi 退出”的验收标准，并附带验证了重启入口可见。
- 证据：`artifacts/accept-m/`（关闭弹窗）、`artifacts/accept-n/`（DSH 页面）、`artifacts/accept-p/`（崩溃后界面）。
- 仍未验证：真实模型回复、应用内运行时升级 UI、高 DPI/多显示器、IME、DSH 聚焦快捷键。

## 44. 自动化原生验收：DSH 崩溃后重启恢复

2026-09-11（本地时间，debug 构建）：

- 流程：打开 DSH → 确认渲染正常 → `taskkill /F` 杀掉应用的直接子进程（托管 `node.exe` 跑 `@deepseek-ai/dsh/lib/bin.js web`）→ 界面显示 `DSH exited with code 1` + “重启 DSH” → 点击该按钮 → 应用重新拉起 DSH 子进程。
- 结果：点击后重新出现子进程（children_after=1），DSH 页面重新渲染（侧栏/新会话/工作区/输入区均正常），应用未退出。第一次尝试的点击坐标略偏未命中按钮，修正坐标后确认重启链路正常。
- 持久化核对：DSH profile 目录（`%APPDATA%\com.deeppi.desktop\agents\dsh`）含 `profiles/`、`storages/`、`settings.yaml`，且 `profiles/web/node_modules/dshmarket` 版本为 **1.40.0**（应用自动启用固定版本 dshmarket）；重启后目录不变。
- 证据：`artifacts/accept-q/`（崩溃页）、`artifacts/accept-r/`（重启后恢复）。
- 仍未验证：DSH 页内“设置/插件市场”具体交互、真实模型回复、应用内运行时升级 UI、高 DPI/多显示器、IME、DSH 聚焦快捷键。

## 45. 自动化原生验收：Pi 扩展市场（真实目录搜索与范围切换）

2026-09-11（本地时间，debug 构建）：

- 设置 → Pi 扩展页正常渲染：搜索框、官方目录计数、已安装面板、全局/项目范围切换。
- 真实目录搜索：输入 `todo` 后从 `pi.dev` 返回 50 条结果，卡片包含包名、类型（extension / Pi Package）、描述、月下载量（如 `866.3K/月`、`400.5K/月`）与发布日期，并带“安装”按钮；结果包含 `pi-mcp-adapter`、`pi-web-access`、`pi-subagents`、`@companion-ai/feynman`、`@juicesharp/rpiv-ask-user-question`、`@juicesharp/rpiv-todo` 等真实 Pi 包。
- 范围切换：点“项目”后按钮高亮切换，右侧面板标题由“已安装 · DeepPi 托管”变为“已安装 · 当前项目”（本次项目无项目级包，显示“暂无已安装 Package”符合预期）；切回“全局”正常。
- 证据：`artifacts/accept-s/`（市场搜索）、`artifacts/accept-t/`（项目范围）。未执行安装/卸载写入操作，以免改变真实扩展配置；安装/更新/卸载仍待人工或后续授权验证。
- 仍未验证：真实安装/卸载 Pi Package、DSH 页内“设置/插件市场”具体交互、真实模型回复、应用内运行时升级 UI、高 DPI/多显示器、IME、DSH 聚焦快捷键。
