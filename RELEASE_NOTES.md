# DeepPi v1.0.3

DeepPi 是把 Pi Coding Agent 与 DeepSeek Harness（DSH）整合到同一工作台的 Windows 桌面应用：无需预装 Node、Pi 或 DSH，运行时由应用自行托管，不干扰系统已有开发环境。

## 本版更新（v1.0.3）

- **休眠会话直接显示历史对话**：应用重启后点击未启动的会话，即刻展示完整历史消息——直接读取 pi 的会话文件，按与在线会话相同的方式渲染（含上下文压缩摘要、自定义与分支消息），无需启动进程、不占并发额度；「显示更早的消息」分页照常可用；输入新提示词回车才启动 Pi 代理，连接后自动接着历史继续。
- **打开会话即恢复历史状态（含模型与推理强度）**：不再需要先发一条提示词才能看到历史；休眠会话在打开时就从会话记录恢复上次使用的模型与推理强度，并显示在输入框工具栏（进程启动前只读展示），回车启动后由 pi 进程状态接管。
- **未命名会话打开即自动生成标题**：按首条用户提示词自动命名的逻辑，现在在打开休眠会话时立即生效，不再等到会话启动或首轮回复结束；手动改过名的会话不受影响。
- **输入框主操作靠右**：提示词增强、停止、发送三个按钮固定在输入框右下角，不再散落在操作行。
- **输入框支持 `/` 命令**：输入 `/` 弹出命令建议（pi 内置命令 + 扩展命令、提示词模板、技能命令），方向键选择、回车执行/发送；内置命令（/compact、/model、/thinking、/name、/copy、/session、/export、/tree）在 DeepPi 内直接生效，扩展/模板/技能命令由 pi 执行，仅终端模式可用的命令会标注来源并给出提示。
- **文件树不再定时刷新**：修复文件监听每 30 秒兜底重扫引起的周期性刷新；重扫间隔改为 5 分钟，且已有内容时后台静默更新，不再反复闪烁「正在读取项目文件…」。
- **长文件名溢出修复**：文件树中的长文件名改为按侧栏宽度省略号截断，不再撑出边框。
- **会话标题悬停提示完整**：自动标题上限从 24 字提升到 200 字（覆盖首条提示词整行），旧版已截断的自动标题在打开会话时自动升级为完整标题；侧栏会话条目也补充了悬停提示。
- **扩展错误回到会话流**：扩展（如 advisor）通过 `ui.notify(…, "error")` 上报的错误改为在会话内容内显示错误卡片，不再飘在输入框上方的提示条；同时修正扩展状态/挂件字段名的解析。
- **带图提示词不再丢图**：修复图片经异步 FileReader 读取、发送时附件还未就绪导致图片静默丢失的问题；读取未完成时发送键保持禁用，避免不完整发送；纯图片（无文字）提示词现在也可发送；同时对齐渲染上限，大图不再只显示占位文本。排队中的带图提示词会在待发送列表标记「含图片」，编辑/移动/撤回时图片一并保留或退回输入框（不再依赖 pi 不返回附件的 clear_queue）。
- **实时状态行不再顶出会话栏**：AI 状态/耗时行（长命令摘要 + 多段时间标签）改为单行省略号截断，悬停可看全文。
- **附带**：包含 v1.0.2 的全部界面修复（会话即开即用、AI 状态与耗时迁入对话流底部、历史面板布局等）；另修复一处导致 CI clippy 失败的遗留告警。

## 下载安装

从 [GitHub Releases](https://github.com/springkai66/deep-pi/releases) 下载 `DeepPi_1.0.3_x64-setup.exe`（或 `DeepPi_1.0.3_x64.msi`）并运行；系统要求 Windows 10/11 x64，WebView2 由安装程序自动部署。已安装用户会通过应用内自动更新收到 v1.0.3 推送。
