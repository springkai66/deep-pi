# DeepPi v1.2.0

工作区布局、模型管理与代理生态的全面升级。

## 新功能

- **工作区布局重构**：文件树移至窗口右侧独立面板；顶部工具栏精简为两个开关（左侧工作区 / 右侧文件栏）。
- **Session 标签增强**：每个标签带关闭按钮，标签栏末尾有「新建 Session」按钮；右键任务标题可**向右/向下分割窗口**，左右两个 Session 并排工作。
- **任务自动命名**：新建任务默认以 Session ID 命名；首轮对话结束后由 AI 根据首条消息自动提炼任务标题。
- **对话窗口**：
  - 推理强度下拉，选项来自当前模型实际支持的级别（非固定列表）；
  - 模型选择按供应商分组显示；
  - 模型权限面板：列出 Pi 全部 8 个内置工具（read/bash/powershell/edit/write/grep/find/ls），勾选即生效；
  - Token 统计条：输入/输出 token、缓存命中率、会话花费，点击查看明细与上下文占用。
- **Provider 与模型管理重构**：Provider 列表可展开；模型带勾选框（保存后只显示已选模型，点「拉取所有模型」展开全部）；右侧为模型详情面板（总上下文窗口、最大输出、推理强度、价格、模型级 API 类型覆盖）；「自动填入」一键补齐官方参数，「测试连通」对模型发真实请求（`openai-responses` 端点 404 时自动回退 chat/completions）；保存成功后按钮置灰并提示。
- **MCP 服务与 Skills 管理**：设置新增两个分类，支持全局/项目双范围列出、添加、删除（MCP 写 `mcp.json`，Skills 写 `skills/<名称>/SKILL.md`）。
- **主题系统**：新增 Windows XP 主题（Luna 蓝 / Royale 黑蓝），与浅色/深色/跟随系统自由组合；修复亮色主题下会话标签栏、归档分隔线等处的硬编码深色。
- **组件更新进度条**：安装/更新 Node、Pi、DSH、dshmarket 时显示阶段与下载百分比。

## 改进

- DSH 升级到已验证的 `0.1.5-rc.2`（宿主已适配 launch-token 认证与 `/api/<ns>/<method>` RPC 信封）。
- 供应商连通测试改用 `/models` 端点且只有 2xx 才算可达，404 不再误报「可达」。
- 运行中的任务可直接删除（自动先停止）。
- 会话输入框固定在窗格底部，不再出现上方大片空白。

## 安装

1. 从 Releases 下载 `DeepPi_1.2.0_x64-setup.exe`。
2. 运行安装包。安装包会自动安装 WebView2。
3. 首次运行如出现 SmartScreen 提示，选择「更多信息」→「仍要运行」。

## 已知限制

- 安装包未做 Authenticode 代码签名（开源签名通道申请中）。
- 不读取本机安装的 Pi/DSH/Node/npm；只使用 DeepPi 自带的托管运行时（Node 由应用从官方发行包安装并校对 SHA-256）。
- DSH 上游更新仍需通过 DeepPi 兼容验证后才会提供。
- DSH 子 Webview 获得焦点时宿主快捷键不生效。
- 文件侧栏的**内容**搜索需要系统 `PATH` 上有 ripgrep（`rg.exe`）：应用不自带也不自动安装，未安装时该模式会提示「未找到 ripgrep」，文件名搜索不受影响。
