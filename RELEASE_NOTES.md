# DeepPi v1.4.0 · 更新说明

v1.4.0 重做桌宠任务环的几何与交互，修掉两个会让桌宠「罢工」的根因；同时移除左侧控制柱，工作区切换上移顶栏。

## 桌宠与任务环

- **修复任务气泡永不显示**：任务环组件在挂载时因效应自读写触发 Svelte 的 `effect_update_depth_exceeded`，整棵反应式图被毒化——气泡永远渲染不出来，加载角标定格在「正在读取任务…」，收环也一并失灵。现已修根因。
- **修复浅色模式下桌宠与浮层变白底**：浅色主题的一条高优先级规则把两个浮窗的 `body` 强制刷成不透明背景，桌宠被自己的窗口挡住。现在两个窗口在任何配色下都保持完全透明。
- **窗口贴合本体**：桌宠窗口从 210×240 收到 99×119（自定义形象按不透明边界动态贴合），去掉顶部拖动条（抓本体即可拖动），关闭按钮待在顶部留白带里、不再压住本体。
- **任务环按时钟刻度分布**：气泡均匀铺满一圈、互不重叠，半径按数量反推——气泡宁可窄一点也不把环推远，悬停时明显更贴近桌宠。
- **最多显示 20 个任务**：超出部分汇总为「还有 N 个任务」角标。
- **点击气泡跳回主窗口**：点气泡主体即在主窗口打开该任务——Pi 任务进任务面板，DSH 任务切到 DSH 工作区；只有真正可点的气泡才接管鼠标，其余区域照常穿透。
- **设置页显示版本号**：设置 → 常规底部新增「关于」，显示当前应用版本。

## 工作区与导航

- **移除左侧控制柱**：DSH 工作区不再有那条 8px 窄条（鼠标路过窗口左缘即误触发，并把原生 Webview 随栅格动画来回拽动）。
- **工作区切换上移顶栏**：顶栏左侧分段控件切换 Pi / DSH，设置入口移至顶栏工具区；顶栏高度仍为 32px，内容区不缩反增（Pi +48px、DSH +8px）。
- **新增快捷键**：`Ctrl+1` 切到 Pi 工作区，`Ctrl+2` 切到 DSH 工作区。

## 升级

在应用内打开 设置 → 应用更新，或从 [GitHub Releases](https://github.com/springkai66/deep-pi/releases) 下载 `DeepPi_1.4.0_x64-setup.exe`（或 `DeepPi_1.4.0_x64.msi`）。

## 既有核心功能（自 v1.0.0）

- **Pi 智能体对话**：多任务并行；同一会话可在结构化对话与 Pi 原生终端之间切换并保留完整上下文；流式回复、思考过程、执行阶段与耗时实时可见；未启动的会话直接打开即可浏览完整历史；带图提示词支持粘贴/拖入与排队/引导投递。
- **全局网络代理**：跟随系统 / 直连 / 手动（地址 + 例外地址），作用于 Pi/DSH 子进程、运行时下载、扩展市场、模型请求与应用内更新，内置连通性测试。
- **命令终端**：独立临时 Shell，支持 PowerShell、PowerShell 7、Bash 和 CMD，工作目录跟随当前项目。
- **DSH 工作区**：同一应用内加载 DeepSeek Harness 原生界面，支持会话与插件市场。
- **项目文件**：文件树、文件名/内容搜索、只读预览、内置编辑器、差异比较和恢复副本。
- **Git 工作流**：查看变更、分类差异、暂存与取消暂存、提交、推送和远程状态核对。
- **任务看板与四象限清单**：独立浮窗管理 Pi 任务（打开、停止、重启、归档、删除）；四象限清单支持增删勾选与持久化。
- **扩展市场**：Pi Package / 扩展 / Skill / MCP 服务检索与安装，模型参数可从 pi.dev/models 自动补全，官方账号 OAuth 登录。
- **运行时与更新**：应用内管理 Node、Pi、DSH 的安装、升级、回滚与诊断；应用内自动更新经签名校验。
- **外观与多语言**：简体中文 / 繁体中文 / English，浅色、深色与跟随系统主题，应用、会话与代码字体可调。

系统要求 Windows 10/11 x64，WebView2 由安装程序自动部署。
