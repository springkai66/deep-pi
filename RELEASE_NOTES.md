# DeepPi v1.0.0

版本号重置后的首个正式版：新会话按项目沿用上次的模型与推理强度，应用图标全面重绘。

## 变更

- **新功能：新会话沿用上次选择**。在某个项目里手动选过的「模型 + 推理强度」会按项目分别记住：该项目之后新建的会话自动应用这份选择，不再回落到默认模型。记住的模型已不可用或推理档位已失效时静默跳过对应项；恢复已有会话（有历史消息）完全不受影响；TUI 终端模式不涉及。选择随任务数据库按项目存储，移除项目后重新添加同一目录仍沿用。
- **新图标**。应用图标重绘为紫蓝渐变底 + 白色 π 字符，并重新生成全部平台图标（ico / icns / PNG 各尺寸），安装包、任务栏与资源管理器统一呈现。
- **对话体验**。生成失败留下的空白 assistant 占位轮次不再渲染（错误仍由横幅展示）；回复生成中也允许切换模型与推理强度。
- **任务看板浮窗**。独立看板窗口的窗口权限、样式与文案收尾。

## 升级说明

- **版本号重置**：此前发布的 v1.0.0–v1.0.3 已全部撤回，从 1.0.0 重新开始计数。本版本包含此前各版本的全部修复；已安装旧测试版的用户直接覆盖安装即可。
- 从本版本起，每个版本发布都有独立且互不覆盖的下载附件；「stable」条目仅作为应用内自动更新的指针，始终指向最新版。
