# DeepPi v1.0.0 · 更新说明

版本号从 v1.0.0 重新开始，此前的版本号全部废弃。这一版重做了网络代理：**默认「跟随系统」，只保留「手动设置」**，并让已打开的 Pi/DSH 会话在切换代理后立即跟随，无需重启。

## 网络代理（重点）

- **默认跟随系统**：以 **Windows 当前启用的系统代理设置**为准（WinHTTP/Internet 设置），并且**按请求目标**解析——支持 `http=` / `https=` 按协议分流、`ProxyOverride` 例外列表、`<local>`，以及 **PAC / WPAD 自动配置**。
- **移除「直连」模式**：只保留「跟随系统」与「手动设置」两项；旧配置里保存的「直连」会在加载与保存时自动迁移为「跟随系统」。
- **不再被残留配置劫持**：系统代理已关闭时，进程启动时继承的 `HTTP_PROXY`/`HTTPS_PROXY`，以及注册表里 `ProxyEnable=0` 时残留的 `ProxyServer`，**都不再被当作系统设置**。这正是此前「已经在系统里关了代理，应用仍被钉在一个没人监听的端口上」的根因。
- **切换即时生效，无需重启会话**：Pi/DSH 子进程拿到的是本机回环中继的**恒定地址**，切换模式或改变系统代理后，新请求立即走新路径。唯一例外是手动填写 `https://` 上游代理（中继只承载 `http://` 上游）。
- **失败绝不静默绕过**：上游代理不可达、配置无法安全表达（需要认证的代理、仅 SOCKS 等）时**明确报错**（中继返回 502），不会悄悄改走直连把流量漏出去。
- **「测试连接」与真实请求走同一条构造路径**，并如实反映系统当前的路由决定——不会再出现「按钮报连接成功、DSH 却全断」。
- **例外地址始终包含本地回环**（localhost / 127.0.0.1 / ::1 / [::1]），本地 Provider、DSH 本地 API 不会因为开启代理而失联。
- 代理相关失败现在**可从日志定位**：重试日志带失败类别与脱敏后的代理来源（如 `category=tcp_connect mode=system route=proxy:127.0.0.1:7890`），运行时更新检查也会逐组件记录失败原因。日志中的 URL 凭据（userinfo）与查询串取值一律脱敏。

## 修复

- **DSH 插件市场更新检查长期失败**：`dshmarket` 的 npm 元数据达 568 KB，超过 512 KB 的响应读取上限，导致它的更新检查在所有镜像上都失败、并且因为「有一个组件出错就不写缓存」而让界面长期显示离线缓存。现改用 npm 缩写元数据并把上限提到 8 MB——实测检查耗时从 27.4 秒降到 1.3 秒，错误数由 1 归零。
- **旧配置不会再让应用无法启动**：若历史配置里存有「格式非法的手动代理地址」（旧版本只做字符串校验，端口笔误即可写入），新版本会把它迁移为「跟随系统」并记日志，而不是在启动时报错。

## 升级

在应用内打开 设置 → 应用更新，或从 [GitHub Releases](https://github.com/springkai66/deep-pi/releases) 下载 `DeepPi_1.0.0_x64-setup.exe`（或 `DeepPi_1.0.0_x64.msi`）。

> 从 v1.3.x / v1.4.0 升级后，代理设置会自动变为「跟随系统」；若此前填过手动代理地址，它仍保留在「代理地址」里，切到「手动设置」即可继续使用。

## 既有核心功能

- **Pi 智能体对话**：多任务并行；同一会话可在结构化对话与 Pi 原生终端之间切换并保留完整上下文；流式回复、思考过程、执行阶段与耗时实时可见；未启动的会话直接打开即可浏览完整历史；带图提示词支持粘贴/拖入与排队/引导投递。
- **全局网络代理**：跟随系统 / 手动设置（地址 + 例外地址），作用于 Pi/DSH 子进程、运行时下载、扩展市场、模型请求与应用内更新，内置连通性测试。
- **命令终端**：独立临时 Shell，支持 PowerShell、PowerShell 7、Bash 和 CMD，工作目录跟随当前项目。
- **DSH 工作区**：同一应用内加载 DeepSeek Harness 原生界面，支持会话与插件市场。
- **项目文件**：文件树、文件名/内容搜索、只读预览、内置编辑器、差异比较和恢复副本。
- **Git 工作流**：查看变更、分类差异、暂存与取消暂存、提交、推送和远程状态核对。
- **任务看板与四象限清单**：独立浮窗管理 Pi 任务（打开、停止、重启、归档、删除）；四象限清单支持增删勾选与持久化。
- **扩展市场**：Pi Package / 扩展 / Skill / MCP 服务检索与安装，模型参数可从 pi.dev/models 自动补全，官方账号 OAuth 登录。
- **运行时与更新**：应用内管理 Node、Pi、DSH 的安装、升级、回滚与诊断；应用内自动更新经签名校验。
- **桌宠与任务环**：任务气泡按时钟刻度环绕桌宠，点击气泡跳回对应任务；半径可设置，浮层点击穿透。
- **外观与多语言**：简体中文 / 繁体中文 / English，浅色、深色与跟随系统主题，应用、会话与代码字体可调。

系统要求 Windows 10/11 x64，WebView2 由安装程序自动部署。
