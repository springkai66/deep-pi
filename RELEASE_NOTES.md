# DeepPi v1.2.0 · 更新说明

v1.2.0 聚焦交互细节：桌宠可直接管理任务，任务侧栏一眼看清当前会话，对话历史面板不再裁剪闪烁，会话重建保留历史不闪屏。

## 新增功能

- **桌宠任务气泡**：悬停桌宠弹出独立任务浮层，每条任务可直接停止（X）或继续（▶），无需回到主窗口；气泡为长条形、宽度随标题自适应（200px 下限～浮层宽度上限），超长省略号截断；停止/继续委托主窗口执行，与停止/重启/关闭/模式切换共享状态锁并校验 runId 防串台；DSH 任务仅展示（无单任务 API，按钮禁用并附说明）。
- **侧栏当前会话高亮**：任务侧栏以选中态（主题色背景 + 左侧竖条 + 标题变色）标出当前正在交互的 Session，「进行中 / 已完成 / 归档」列表均生效，切换会话时高亮实时跟随，与任务看板的卡片高亮风格一致。

## 改进与修复

- **对话历史面板**：面板高度固定（矮窗格按实测剩余空间收口防裁剪），不再随轮次数量伸缩；标题固定顶部，轮次列表超出时在面板内滚动，打开面板或 Alt+↑/↓ 切换时激活项自动滚入可视区且不波及聊天窗格；移除逐轮「耗时」行，正文消息标签的耗时胶囊样式恢复原设计。
- **会话重建不闪烁**：同一会话重建（休眠→回车启动、重连、重启）时保留已加载历史消息，不再先清空导致整屏刷新闪烁。
- **自动发送就绪**：连接就绪前输入的自动发送草稿现在会等待连接初始化完成后再发出，不再被静默吞掉、无需再按一次回车。
- **桌宠默认形象**：改为原图抠白底的透明动图（pet-default.gif），观感更自然；自定义形象功能不变。

## 升级

在应用内打开 设置 → 应用更新，或从 [GitHub Releases](https://github.com/springkai66/deep-pi/releases) 下载 `DeepPi_1.2.0_x64-setup.exe`（或 `DeepPi_1.2.0_x64.msi`）。

## 既有核心功能（自 v1.0.0）

- **Pi 智能体对话**：多任务并行；同一会话可在结构化对话与 Pi 原生终端之间切换并保留完整上下文；流式回复、思考过程、执行阶段与耗时实时可见；未启动的会话直接打开即可浏览完整历史；带图提示词支持粘贴/拖入与排队/引导投递。
- **全局网络代理**：跟随系统 / 直连 / 手动（地址 + 例外地址），作用于 Pi/DSH 子进程、运行时下载、扩展市场、模型请求与应用内更新，内置连通性测试。
- **命令终端**：独立临时 Shell，支持 PowerShell、PowerShell 7、Bash 和 CMD，工作目录跟随当前项目。
- **DSH 工作区**：同一应用内加载 DeepSeek Harness 原生界面，支持会话与插件市场。
- **项目文件**：文件树、文件名/内容搜索、只读预览、内置编辑器、差异比较和恢复副本。
- **Git 工作流**：查看变更、分类差异、暂存与取消暂存、提交、推送和远程状态核对。
- **任务看板与四象限清单**：独立浮窗管理 Pi 任务（打开、停止、重启、归档、删除）；四象限清单支持增删勾选与持久化。
- **扩展市场**：Pi Package / 扩展 / Skill / MCP 服务检索与安装，模型参数可从 pi.dev/models 自动补全，支持官方账号 OAuth 登录。
- **运行时与更新**：应用内管理 Node、Pi、DSH 的安装、升级、回滚与诊断；应用内自动更新经签名校验。
- **外观与多语言**：简体中文 / 繁体中文 / English，浅色、深色与跟随系统主题，应用、会话与代码字体可调。

系统要求 Windows 10/11 x64，WebView2 由安装程序自动部署。
