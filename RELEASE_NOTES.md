# DeepPi v1.0.5

DeepPi 是把 Pi Coding Agent 与 DeepSeek Harness（DSH）整合到同一工作台的 Windows 桌面应用：无需预装 Node、Pi 或 DSH，运行时由应用自行托管，不干扰系统已有开发环境。

## 本版更新（v1.0.5）

- **修复「对话历史」面板的显示错误**：轮次列表里的「耗时」之前沿用了消息标签的胶囊样式，在纵向布局里被拉伸成横跨整行的长条，视觉上挤在下一轮文字上方。现改为内容宽度的小字，不再与相邻轮次重叠；胶囊样式的耗时标签在会话流中保持不变。

## 下载安装

从 [GitHub Releases](https://github.com/springkai66/deep-pi/releases) 下载 `DeepPi_1.0.5_x64-setup.exe`（或 `DeepPi_1.0.5_x64.msi`）并运行；系统要求 Windows 10/11 x64，WebView2 由安装程序自动部署。已安装用户会通过应用内自动更新收到 v1.0.5 推送。
