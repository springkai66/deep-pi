# DeepPi v1.3.0 · 更新说明

v1.3.0 重做了桌宠任务环，并根治了两类「点了没反应」的界面冻结：会话初始化被反复触发导致模型选项不出现、回车不发送；启动/退出链路被无超时的锁卡死导致整个界面不可交互。

## 新增功能

- **桌宠任务环**：悬停桌宠，任务气泡围着本体依次绽放成一圈；浮层窗口为固定正方形、中心与本体中心重合，气泡沿圆周排布，超出后折成「还有 N 个」角标，多显示器下半径自动收进屏幕。关闭时依次淡出，退场途中鼠标回到环上会原地复活；打开动画每次都从零重播。任务结束（完成/失败/被停止）后气泡停留数秒给出结果提示再淡出；动作失败按任务就地展示原因并可就地重试，不再污染整个环。

## 改进与修复

- **模型选项与回车发送失效**：修复会话重建时初始化被历史快照反复触发的问题——此前界面可能停在「正在连接 Pi 会话…」，模型选择器不出选项、输入提示词按回车不发送。现在只有任务/会话变化才会重建连接，休眠草稿仍在连接就绪后自动发送一次。
- **点任务、点关闭没反应**：Pi 启动请求限时 60 秒，退出链路（设置落盘、文件编辑锁、停止 Pi/DSH、销毁窗口）每步限时 15 秒——任一步卡住都会报错并释放界面、允许重试，不再出现「界面还在、点什么都没反应」的永久冻结；「询问关闭方式」等待人的选择，仍不设时限。
- **桌宠交互简化**：任务环只保留「结束」动作，「继续」请回到主窗口；DSH 任务仅提示到主窗口操作。悬停期间桌宠本体窗口的尺寸、位置与图片节点保持不变，不再因透明 WebView 改尺寸而闪烁。

## 升级

在应用内打开 设置 → 应用更新，或从 [GitHub Releases](https://github.com/springkai66/deep-pi/releases) 下载 `DeepPi_1.3.0_x64-setup.exe`（或 `DeepPi_1.3.0_x64.msi`）。

## 既有核心功能（自 v1.0.0）

- **Pi 智能体对话**：多任务并行；同一会话可在结构化对话与 Pi 原生终端之间切换并保留完整上下文；流式回复、思考过程、执行阶段与耗时实时可见；未启动的会话直接打开即可浏览完整历史；带图提示词支持粘贴/拖入与排队/引导投递。
- **全局网络代理**：跟随系统 / 直连 / 手动（地址 + 例外地址），作用于 Pi/DSH 子进程、运行时下载、扩展市场、模型请求与应用内更新，内置连通性测试。
- **命令终端**：独立临时 Shell，支持 PowerShell、PowerShell 7、Bash 和 CMD，工作目录跟随当前项目。
- **DSH 工作区**：同一应用内加载 DeepSeek Harness 原生界面，支持会话与插件市场。
- **项目文件**：文件树、文件名/内容搜索、只读预览、内置编辑器、差异比较和恢复副本。
- **Git 工作流**：查看变更、分类差异、暂存与取消暂存、提交、推送和远程状态核对。
- **任务看板与四象限清单**：独立浮窗管理 Pi 任务（打开、停止、重启、归档、删除）；四象限清单支持增删勾选与持久化。
- **扩展市场**：Pi Package / 扩展 / Skill / MCP 服务检索与安装，模型参数可从 pi.dev/models 自动补全，支持官方账号 OAuth 登录。
- **运行时与更新**：应用内管理 Node、Pi、DSH 的安装、升级、回滚与诊断；应用内自动更新经签名校验。
- **外观与多语言**：简体中文 / 繁体中文 / English，浅色、深色与跟随系统主题，应用、会话与代码字体可调。

系统要求 Windows 10/11 x64，WebView2 由安装程序自动部署。
