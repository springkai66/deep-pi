# DeepPi v1.1.0 · 更新说明

v1.1.0 聚焦任务执行的可视化与网络韧性：任务完成有系统通知，文件修改在对话中直接呈现差异对比，网络瞬断自动重试，另新增可定制的桌宠。

## 新增功能

- **任务完成系统通知**：任务完成或失败时发送系统级通知，应用最小化、切到后台也会提示；取消的任务不误报，同一运行不重复通知；可在 设置 → 通知与桌宠 中关闭。通知按当前界面语言渲染。
- **桌宠**：桌面上的透明置顶小宠物，随任务状态做动作——工作中加速浮动、任务完成跳跃庆祝、失败时沮丧抖动。形象支持两种来源：内置默认图片、本地图片（PNG/JPG/WebP/GIF/SVG，按文件头校验格式）。位置可拖动并记忆（支持多显示器与负坐标），启动显示可在设置中配置，主窗口工具栏可随时打开。
- **系统托盘**：运行时在 Windows 通知区域（任务栏右下角）常驻与应用图标一致的托盘图标：左键单击显示/隐藏主窗口，右键菜单提供「显示主窗口」与「退出」；退出复用既有关闭流程（活动任务确认、设置落盘、任务清理后才退出），托盘菜单文案随界面语言即时切换。
- **会话内差异对比**：任务修改文件（edit/write）时，对话流内直接显示行级差异对比——文件路径、变更类型、增删统计、带行号的着色差异行；长上下文自动折叠为「已折叠 N 行」，超大文件安全降级提示；取消的任务不显示伪差异。
- **网络自动重试**：模型调用、模型列表、扩展市场、运行时更新检查、应用更新检查遇网络瞬断 / DNS 抖动 / 5xx / 429 自动退避重试（共 3 次尝试，600ms → 1.5s）；确定性失败（4xx、配置错误）仍快速报错；429 的 Retry-After 头被尊重（超 10s 预算则直接报错）；非幂等的模型调用只重试连接建立失败与网关类状态，避免重复计费。

## 改进与修复

- **安装包图标**：NSIS 安装程序（`DeepPi_x.y.z_x64-setup.exe`）的文件图标改为与应用图标一致（此前为通用图标）。
- **代理设置**：「跟随系统」修复为支持 Windows 系统代理（注册表）回退；「直连」现在也会阻止应用内更新器走系统代理；OAuth 桥接的直连语义修正（直连不再注入代理）。
- **扩展错误横幅**：扩展功能恢复（发出后续有效信号）后自动隐藏，支持手动关闭，不再长期滞留。
- **代理回归测试**：新增独立进程集成测试，覆盖模式切换时的环境保存/恢复与系统代理解析。

## 升级

在应用内打开 设置 → 应用更新，或从 [GitHub Releases](https://github.com/springkai66/deep-pi/releases) 下载 `DeepPi_1.1.0_x64-setup.exe`（或 `DeepPi_1.1.0_x64.msi`）。

## 既有核心功能（自 v1.0.0）

- **Pi 智能体对话**：多任务并行；同一会话可在结构化对话与 Pi 原生终端之间切换并保留完整上下文；流式回复、思考过程、执行阶段与耗时实时可见；未启动的会话直接打开即可浏览完整历史；带图提示词支持粘贴/拖入与排队/引导投递。
- **全局网络代理**：跟随系统 / 直连 / 手动（地址 + 例外地址），作用于 Pi/DSH 子进程、运行时下载、扩展市场、模型请求与应用内更新，内置连通性测试。
- **命令终端**：独立临时 Shell，支持 PowerShell、PowerShell 7、Bash 和 CMD，工作目录跟随当前项目。
- **DSH 工作区**：同一应用内加载 DeepSeek Harness 原生界面，支持会话与插件市场。
- **项目文件**：文件树、文件名/内容搜索、只读预览、内置编辑器、差异比较和恢复副本。
- **Git 工作流**：查看变更、分类差异、暂存与取消暂存、提交、推送和远程状态核对。
- **任务看板与四象限清单**：独立浮窗管理 Pi 任务（打开、停止、重启、归档、删除）；四象限清单支持增删勾选与持久化。
- **扩展市场**：Pi Package / 扩展 / Skill / MCP 服务检索与安装，模型参数可从 pi.dev/models 自动补全，支持官方账号 OAuth 登录。
- **运行时与更新**：应用内管理 Node、Pi、DSH 的安装、升级、回滚与诊断；应用内自动更新经签名校验。
- **外观与多语言**：简体中文 / 繁体中文 / English，浅色、深色与跟随系统主题，应用、会话与代码字体可调。

系统要求 Windows 10/11 x64，WebView2 由安装程序自动部署。
