# DeepPi v1.0.3

修复 DSH 插件市场无法出现的问题，并完成一轮全仓代码审查后的重构。

## 变更

- **修复：DSH 设置里看不到「插件市场」**。DSH 只加载 profile `package.json` 中 `dsh.profile.bundles` 列出的插件，而 DeepPi 用托管 npm 直接把 dshmarket 装进 `node_modules`，从未把它加入该列表 —— 包体在磁盘上，插件却始终不被加载，重启多少次都无效。现在安装后会自动完成这一步，并在每次启动 DSH 前按实际安装状态自愈（列表与实际不符时以实际为准）。已经装过 dshmarket 的机器升级后无需重装，直接打开 DSH → 设置 → 插件市场即可。
- **修复：鉴权构造可能不一致**。「测试连接」与实际模型调用（模型列表、模型探测）此前各自复制了一份 provider 请求头构造，协议细节（如 Anthropic 版本头）只改一处就会让两者行为分叉，出现假阳性/假阴性。现已收敛为一处。
- **性能：技能与 MCP 配置读写不再阻塞界面**。`agent_config.rs` 的 6 个命令（列出/保存/删除 MCP 服务与 Skill）此前同步执行文件系统操作，其中「列出 Skill」会遍历目录并逐个读取 `SKILL.md`、「删除 Skill」是递归删除；慢速磁盘上会直接卡住 WebView。现已与其余运行时命令一致，转入阻塞线程池执行。
- **维护性：Git 命令样板收敛**。8 个 Git 入口此前各自重复「解析项目路径 → 登记操作 ID → 转入阻塞线程 → 打开仓库」的样板，且信任策略参数已出现分化；现集中为一处，信任策略成为显式参数，新增命令不会再漏掉主窗口校验、操作 ID 登记或超时预算。远程跟踪引用同步仍保留其 180 秒预算。
- **维护性：运行环境组件标识集中定义**；图形界面中 4 个含真实身份的列表补上 `{#each}` key，避免列表重排时丢失条目内部状态。
- **清理**：移除声明但从未引用的 `codemirror` 依赖（代码只用 `@codemirror/*` 子包）。

## 升级说明

- 从 1.0.2 升级无需任何手动步骤：运行时会话、项目与设置均保留。
- 若你的 DSH 之前已装过 dshmarket（无论通过 DeepPi 运行时页还是手动），升级后启动 DSH 即会自动修正插件列表。

## 安装

1. 从 Releases 下载 `DeepPi_1.0.3_x64-setup.exe`，安装时自选安装目录（建议非 C 盘）。
2. 运行安装包。安装包会自动安装 WebView2。
3. 首次运行如出现 SmartScreen 提示，选择「更多信息」→「仍要运行」。

---

## DeepPi v1.0.2

稳定性与体验修复：运行时下载不再卡死，新增运行服务一键重启。

## 变更

- **安装不再卡死**：托管运行时（Pi / DSH / dshmarket）下载改为按候选源顺序自动切换：npmmirror（阿里 CDN，国内快且稳）→ npm 官方 → 腾讯镜像。单源失败立即换下一个，不再无提示等待。
- **下载可见性**：安装期间每 10 秒上报心跳（当前源 + 已用时），换源时提示「X 失败，改用 Y 重试」。
- **Node 发行包**：SHASUMS 与归档下载同样支持 npmmirror 镜像回落，SHA-256 校验仍对内容生效。
- **运行服务重启**：「设置 → 运行时与更新」新增运行服务区，可一键重启所有运行中的 Pi 任务（会话保留）与重启/启动 DSH；更新或回滚运行时后一键切换到新版本。

## 升级说明

- 从 1.0.0/1.0.1 升级：运行时安装位置已改为安装目录下的 `runtimes` 子文件夹，旧版运行时不会自动迁移；升级后请到「设置 → 运行时与更新」重新安装 Node → Pi → DSH，确认可用后可手动删除旧目录。

## 安装

1. 从 Releases 下载 `DeepPi_1.0.2_x64-setup.exe`，安装时自选安装目录（建议非 C 盘）。
2. 运行安装包。安装包会自动安装 WebView2。
3. 首次运行如出现 SmartScreen 提示，选择「更多信息」→「仍要运行」。

---

## DeepPi v1.0.1

修复与改进版本：运行时不再写入 C 盘 AppData。

### 变更

- **运行时安装位置**：托管运行时（Node / Pi / DSH / dshmarket）改为安装在 DeepPi 安装目录下的 `runtimes` 子文件夹，跟随安装盘符，不再写入 `%LOCALAPPDATA%`。把 DeepPi 装在非 C 盘时，运行时也全部留在同一块盘上。
- **npm 缓存位置**：应用内安装/升级 Pi、DSH、dshmarket 时，npm 下载缓存显式指向应用自己的 cache 目录，不再写入 C 盘用户目录。
- **错误提示**：DeepPi 被安装在受保护目录（如 Program Files）导致运行时目录无法创建时，给出可操作的报错说明。

### 升级说明

- 从 1.0.0 升级的用户：旧版安装在 `%LOCALAPPDATA%\com.deeppi.desktop\runtimes` 的运行时不会被自动迁移。升级后请到「设置 → 运行时与更新」重新安装 Node → Pi → DSH（确认框会说明将要下载的版本），确认可用后可手动删除旧目录。

### 安装

1. 从 Releases 下载 `DeepPi_1.0.1_x64-setup.exe`，安装时自选安装目录（建议非 C 盘）。
2. 运行安装包。安装包会自动安装 WebView2。
3. 首次运行如出现 SmartScreen 提示，选择「更多信息」→「仍要运行」。

---

## DeepPi v1.0.0

DeepPi 的首个发布版本：在同一个 Windows 桌面应用中使用 Pi Coding Agent 与 DeepSeek Harness。

### 主要能力

- **Pi 工作区**：多个 Pi 任务并行，原生 TUI 终端与结构化 RPC 对话可互相切换，共享同一会话。
- **会话管理**：标签带关闭按钮与新建按钮；右键任务标题可**向右/向下分割窗口**，两个会话并排工作；首轮对话后由 AI 自动提炼任务标题。
- **对话窗口**：推理强度下拉（来自模型实际支持的级别）；模型按供应商分组；Token 统计条（输入/输出、缓存命中率、会话花费与上下文占用，点击查看明细）。
- **项目文件**：右侧文件树、文件名与内容搜索、只读预览、内置编辑器、差异比较与恢复副本。
- **Git 审阅**：状态分类、差异查看、显式暂存/取消暂存、提交与推送，不默认强推。
- **Provider 与模型管理**：Provider 列表可展开；模型带勾选框（保存后只显示已选模型，点「拉取所有模型」展开全部）；模型详情可编辑总上下文窗口、最大输出、推理强度、价格与模型级 API 类型覆盖；「自动填入」从 pi.dev 官方目录补齐参数，「测试连通」对模型发真实请求。
- **MCP 与 Skills 管理**：设置中可列出、添加、删除，支持全局与项目两种范围。
- **DSH 原生界面**：同窗口加载 DSH Web UI，会话、设置与插件市场（dshmarket）可用。
- **Pi 扩展市场**：搜索、安装、更新与卸载 Pi Package，安装前确认来源。
- **运行时升级**：应用内升级 Node.js / Pi / DSH 与 dshmarket，安装过程显示阶段与下载进度；激活后保留上一版本，失败或想回退时可一键回滚（回退深度为一级：只记录「上一活跃版本」）。
- **主题**：Windows 11 与 Windows XP 两套主题，各自支持跟随系统 / 浅色 / 深色。

### 安装

1. 从 Releases 下载 `DeepPi_1.0.0_x64-setup.exe`。
2. 运行安装包。安装包会自动安装 WebView2。
3. 首次运行如出现 SmartScreen 提示，选择「更多信息」→「仍要运行」。

### 首次使用

不需要预先安装 Node、Pi 或 DSH——运行时由应用自己管理，不与电脑上已装的环境互相影响。

1. **配置模型凭据**：打开「设置 → 模型设置」填入 Provider 的 API Key（密钥存入 Windows Credential Manager，不写入配置文件）。
2. **打开项目**：在左栏点「添加项目目录」选择代码目录。
3. **新建任务**：点「新建任务」创建终端或对话任务；两种模式可随时互相切换且共享同一会话。

首次使用某个功能时，应用会按需从官方发行包安装对应的托管运行时（Node / Pi / DSH），确认框会说明将要下载的版本。

### 已知限制

- 安装包未做 Authenticode 代码签名（开源签名通道申请中）。
- 不读取本机安装的 Pi/DSH/Node/npm；只使用 DeepPi 自带的托管运行时（Node 由应用从官方发行包安装并校对 SHA-256）。
- DSH 运行在已验证的 `0.1.5-rc.2`；上游更新需通过 DeepPi 兼容性验证后才会提供。第三方 DSH 插件若声明了更高的 DSH 版本要求，安装后可能导致 DSH 插件树加载失败——应用会显示失败原因，请在 DSH 插件市场中禁用或卸载该插件。
- DSH 子 Webview 获得焦点时宿主快捷键不生效。
- 文件侧栏的**内容**搜索需要系统 `PATH` 上有 ripgrep（`rg.exe`）：应用不自带也不自动安装，未安装时该模式会提示「未找到 ripgrep」，文件名搜索不受影响。
- 本版为首个版本，DeepPi 应用自更新的跨版本升级与回滚验证将在下个版本发布时进行。
- 8 小时长稳与部分原生交互验收尚未完成，详见仓库内 `NATIVE_ACCEPTANCE.md`。
