# DeepPi v1.3.1 · 更新说明

v1.3.1 修复桌宠任务环的一个交互缺陷：任务环浮层会吞掉桌宠本体的鼠标事件，导致桌宠无法拖动、悬停无手型、关闭按钮不出现，且任务环永远停在「正在读取任务…」。

## 改进与修复

- **桌宠恢复可拖动、可悬停**：任务环浮层默认点击穿透，宿主仅在光标落到任务气泡或错误角标上时才接管点击——环打开期间桌宠仍可拖动、悬停手型与关闭按钮正常，点「结束」不再抢当前应用焦点；「显示→吞鼠标→收窗→重开」的闪烁循环一并根除，「一直显示正在读取任务」不复存在。
- **空环有交代**：没有进行中任务时，悬停显示「暂无进行中任务」约 1.5 秒后环自动收起；原地悬停不会反复弹出，移开再悬停才重开。
- **首次悬停即开环**：任务环浮层随桌宠打开预先建好，消除首次悬停的构建延迟。
- **DSH 任务提示**：DSH 任务气泡悬停时提示「请在主窗口操作」。

## 升级

在应用内打开 设置 → 应用更新，或从 [GitHub Releases](https://github.com/springkai66/deep-pi/releases) 下载 `DeepPi_1.3.1_x64-setup.exe`（或 `DeepPi_1.3.1_x64.msi`）。

## 既有核心功能（自 v1.0.0）

- **Pi 智能体对话**：多任务并行；同一会话可在结构化对话与 Pi 原生终端之间切换并保留完整上下文；流式回复、思考过程、执行阶段与耗时实时可见；未启动的会话直接打开即可浏览完整历史；带图提示词支持粘贴/拖入与排队/引导投递。
- **全局网络代理**：跟随系统 / 直连 / 手动（地址 + 例外地址），作用于 Pi/DSH 子进程、运行时下载、扩展市场、模型请求与应用内更新，内置连通性测试。
- **命令终端**：独立临时 Shell，支持 PowerShell、PowerShell 7、Bash 和 CMD，工作目录跟随当前项目。
- **DSH 工作区**：同一应用内加载 DeepSeek Harness 原生界面，支持会话与插件市场。
- **项目文件**：文件树、文件名/内容搜索、只读预览、内置编辑器、差异比较和恢复副本。
- **Git 工作流**：查看变更、分类差异、暂存与取消暂存、提交、推送和远程状态核对。
- **任务看板与四象限清单**：独立浮窗管理 Pi 任务（打开、停止、重启、归档、删除）；四象限清单支持增删勾选与持久化。
- **扩展市场**：Pi Package / 扩展 / Skill / MCP 服务检索与安装，模型参数可从 pi.dev/models 自动补全，官方账号 OAuth 登录。
- **运行时与更新**：应用内管理 Node、Pi、DSH 的安装、升级、回滚与诊断；应用内自动更新经签名校验。
- **外观与多语言**：简体中文 / 繁体中文 / English，浅色、深色与跟随系统主题，应用、会话与代码字体可调。

系统要求 Windows 10/11 x64，WebView2 由安装程序自动部署。
